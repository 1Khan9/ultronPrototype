# COMPACT DECISION DIGEST  research=20/20 adversarial=17/20

## ADVERSARIAL — P0/P1 + refuted/conflict/overclaim verdicts, all MISSING, all NET

### Settings/permission/precedence claims in the enforcement-bun...
NET: SHIP the settings/permissions design largely as researched - the high-stakes anchors all verified against official docs: deny>ask>allow with no allow-exceptions, managed-only authority of disableBypassPermissionsMode/allowManagedPermissionRulesOnly/allowManagedHooksOnly, stop_hook_active + the 8-block cap (REAL - I nearly false-flagged it as hallucinated because the /hooks reference summary omitte...
- [confirmed|P1] defaultMode is inside permissions; valid values are default/acceptEdits/plan/auto/dontAsk/bypassPermissions; defaultMode:'auto' is SILENTLY IGNORED in... | SAFER: Pin defaultMode:'default' in project settings (anchors safe-prompt). Never put 'auto' or 'bypassPermissions' in project/local - silently ignored or refused. Use ~/.claude or manage... | WHY: Confirmed verbatim: 'Claude Code v2.1.142 and later ignore auto from those files so a repository cannot grant itself auto mode. Move it to ~/.claude/settings.json.' The six modes match the official ta...
- [confirmed|P0] The Stop-hook self-deadlock guard is the stop_hook_active boolean in the Stop-hook stdin JSON; a Stop hook that always blocks loops; the harness overr... | SAFER: First line of EVERY Stop/SubagentStop hook: if (input.stop_hook_active) process.exit(0). Do not hardcode the count 8 (override via CLAUDE_CODE_STOP_HOOK_BLOCK_CAP if needed). Back ... | WHY: INITIALLY this looked refuted - the /hooks REFERENCE page summary did NOT surface stop_hook_active. But the hooks-GUIDE page documents it verbatim: 'Claude Code overrides a Stop hook after it blocks 8...
- [confirmed|P1] managed-only keys claudeMd, disableClaudeAiConnectors, strictPluginOnlyCustomization exist; enableAllProjectMcpServers/enabledMcpjsonServers must NOT ... | SAFER: Never set enableAllProjectMcpServers:true in committed settings. Treat all non-managed hooks as advisory-plus; make a GitHub Actions required-status-check the unoverridable gate (i... | WHY: Settings page confirms claudeMd (managed-only), disableClaudeAiConnectors (managed-only), enableAllProjectMcpServers, enabledMcpjsonServers, and disableAllHooks ('Disable all hooks and any custom stat...
- [overclaim|P2] SessionStart command hooks inject context via hookSpecificOutput.additionalContext. | SAFER: In SessionStart/UserPromptSubmit command hooks, write the reground text to stdout on exit 0. Reserve hookSpecificOutput.additionalContext for PreToolUse/PostToolUse/Stop where it i... | WHY: For COMMAND hooks the documented SessionStart injection path is plain STDOUT, not additionalContext: hooks-guide says 'For UserPromptSubmit, UserPromptExpansion, and SessionStart hooks, anything you w...
- [refuted|P2] Stop and SubagentStop hooks can be scoped via a matcher (e.g. agent_type) to changed files / specific agents. | SAFER: Do change-scoping inside the Stop hook body with `git diff --name-only HEAD`. Do not write a Stop/SubagentStop matcher expecting it to filter - it is ignored. | WHY: Hooks-guide matcher-support table (line 638) lists Stop (and UserPromptSubmit, PostToolBatch, WorktreeCreate, CwdChanged, MessageDisplay) as 'no matcher support - always fires on every occurrence.' St...
- [confirmed|P0] The canon ENFORCEMENT.md PostToolUse hook runs `ruff format <file>` (write mode), which reformats the whole 10,700-line legacy orchestrator.py on ever... | SAFER: Use `ruff format --check` (advisory only) + `ruff check --no-fix` on the single tool_input.file_path; grandfather orchestrator.py via extend-per-file-ignores and a mypy [[tool.mypy... | WHY: This is the single biggest functionality-loss/churn hazard. ruff format in write mode rewrites the entire file including untouched lines, violating the surgical-diff requirement, polluting git blame, ...
- [confirmed|P1] Editing canon/settings/hooks files should be gated with permissions.ask (not deny), because the user routinely directs those edits. | SAFER: Do NOT add deny rules for canon/.claude/settings files. Rely on built-in protected-path prompting + an optional advisory PreToolUse additionalContext explaining sensitivity. Reserv... | WHY: Correct in spirit and critical to the no-false-block requirement: a deny on /docs/canon or .claude/settings.json would block legitimate user-directed maintenance. BUT note .claude, .mcp.json, .claude....
MISSING: Protected paths are a major omitted control: .claude, .mcp.json, .claude.json, .pre-commit-config.yaml, .git, shell rc f... | disableAllHooks kill-switch (readable from user/local scope) means within-session hooks are NOT a trustworthy last line ... | The auto-mode classifier already hard-blocks ~20 destructive classes server-side (force-push, push-to-main, git reset --... | The auto-mode rule that broad allow rules (Bash(*), PowerShell(*), wildcarded interpreters, Agent allow rules, package-m... | PreToolUse parameter-matching syntax Tool(param:value) (e.g. Agent(model:opus), Bash(run_in_background:true), Agent(isol... | Symlink permission semantics: deny rules block if EITHER the symlink OR its target matches, while allow rules require BO... | footerLinksRegexes and the statusLine refreshInterval are real low-risk observability surfaces the research under-specif... | The documented Windows pwsh-Stop-hook empty-stdin bug (GH #46601) reinforces Node-over-pwsh specifically for stdin-readi... | No mention of the InstructionsLoaded hook as the correct place to scan a freshly-loaded CLAUDE.md/rules file for injecti... | Embedder/SDK managedSettings with parentSettingsBehavior:'merge' (embedder values can tighten but not loosen) - relevant...

### Hook-event catalog verification (plus high-risk cross-cuttin...
NET: The hook-event catalog in the research is ACCURATE — every exotic event (PostToolBatch, TeammateIdle, UserPromptExpansion, InstructionsLoaded, ConfigChange, WorktreeCreate, SubagentStart, PostToolUseFailure, PermissionRequest, PermissionDenied, Setup, Elicitation/ElicitationResult, MessageDisplay) is confirmed in the current official lifecycle table; no events are hallucinated and the exit-2-block...
- [overclaim|P2] The research digest claims (issue #3983) that PostToolUse hookSpecificOutput.additionalContext is 'confirmed broken' and is MCP-specific, so you must ... | SAFER: Emit the CORRECT schema (hookSpecificOutput.hookEventName='PostToolUse' + additionalContext, or top-level decision:block+reason). Keep a stderr line as belt-and-suspenders, but do ... | WHY: #3983 (opened 2025-07-19, closed-not-planned) is a year-old report that is explicitly NOT MCP-specific (the digest mislabels it), and the reporter's example used a malformed payload (bare {"reason":.....
- [confirmed|P0] stop_hook_active is a real field in Stop/SubagentStop input; a Stop hook that always blocks loops forever; check it first and exit 0 when true. Claude... | SAFER: Author Stop/SubagentStop in Node.js (not pwsh, see Windows bug below), make 'if (input.stop_hook_active) process.exit(0)' the first statement, scope checks to git-changed files of ... | WHY: The hooks-GUIDE page (not the reference, which truncates) explicitly documents the 'Stop hook hits the block cap' failure: 'Claude Code overrides a Stop hook after it blocks 8 times in a row without p...
- [confirmed|P0] On Windows, pwsh/powershell.exe Stop hooks invoked via -File receive EMPTY stdin (IsInputRedirected False), so stop_hook_active/cwd are unavailable; N... | SAFER: Invoke ALL Stop/SubagentStop (and ideally all blocking) hooks as 'node ${CLAUDE_PROJECT_DIR}/.claude/hooks/x.mjs' reading fs.readFileSync(0,'utf8') with a try/catch defaulting to {... | WHY: #46601 (Windows 11, CC v2.1.101, closed-not-planned/stale) confirms Stop hooks get no stdin under pwsh while an identical UserPromptSubmit hook works. For Ultron (Windows) this is P0: a PowerShell Sto...
- [confirmed|P1] defaultMode:'auto' set in .claude/settings.json or settings.local.json is silently ignored since Claude Code v2.1.142 and must live in ~/.claude/setti... | SAFER: Put defaultMode:'default' in project .claude/settings.json (anchors safe-prompt), and document that auto-mode for unattended runs must be set in user/managed settings or via --perm... | WHY: Exact official text: 'Claude Code v2.1.142 and later ignore auto from those files so a repository cannot grant itself auto mode. Move it to ~/.claude/settings.json.' P1 because a bundle that puts defa...
- [confirmed|P1] On entering auto mode, broad allow rules that grant arbitrary code execution are dropped: blanket Bash(*)/PowerShell(*), wildcarded interpreters like ... | SAFER: Express autonomous-run allowances as NARROW rules (Bash(npm test), Bash(git commit *)) that survive auto-mode entry; never depend on Bash(*) for throughput. For Agent workflows, re... | WHY: Official accordion 'How the classifier evaluates actions' lists exactly these dropped rule classes and 'Dropped rules are restored when you leave auto mode.' P1 for the bundle design: any settings.jso...
- [version-uncertain|P2] Auto mode's two-stage classifier has a 0.4% false-positive rate (Stage 1 8.5% recall-tuned, Stage 2 CoT) and requires Opus 4.6+/Sonnet 4.6. | SAFER: Cite auto mode as a research-preview classifier without quoting a precise FP rate in the bundle docs; rely on deterministic deny rules + hooks + CI for guarantees, since the page i... | WHY: The model-requirement (Opus 4.6+/Sonnet 4.6 on API; Opus 4.7/4.8 on Bedrock/Vertex/Foundry) IS confirmed on the official page, but the specific 0.4%/8.5% two-stage numbers are NOT on any official doc ...
- [confirmed|P1] Protected paths (.claude, .git, .mcp.json, .claude.json, .pre-commit-config.yaml, .gitconfig, shell rc files, etc.) are NEVER auto-approved in any mod... | SAFER: Do NOT add deny/ask rules for .claude/** or .pre-commit-config.yaml — the protected-path mechanism already gates them with the right 'Yes, allow Claude to edit its own settings for... | WHY: Official 'Protected paths' section: the listed dirs/files are prompted (default/acceptEdits/plan), routed-to-classifier (auto), denied (dontAsk), and allow rules 'do not pre-approve protected-path wri...
- [confirmed|P1] disableAllHooks:true in .claude/settings.local.json bypasses even managed-tier hooks (issue #26637, HIGH, closed without confirmed fix) — so within-se... | SAFER: Register the five guard checks (no-test-deletion, ruff/mypy on changed files, lockfile freshness, secrets scan) as REQUIRED status checks on main with 'do not allow bypassing'; tre... | WHY: #26637 title 'Security: disableAllHooks bypasses managed organization hooks', status Closed, documents that local disableAllHooks overrides managed hooks contrary to docs. Validates the research's cor...
- [confirmed|P1] WebFetch domain allow/deny rules do NOT prevent Bash curl/wget/Invoke-WebRequest from reaching any URL; the SSRF/egress boundary must be enforced sepa... | SAFER: Keep WebSearch/WebFetch ALLOWED for the kit's deep-research workflows; add scoped Bash(curl *)/Bash(wget *)/PowerShell(Invoke-WebRequest *) denies + a Node SSRF PreToolUse hook. Do... | WHY: Official WebFetch warning: 'using WebFetch alone does not prevent network access. If Bash is allowed, Claude can still use curl, wget, or other tools to reach any URL.' Confirms the research. For the ...
MISSING: Protected-paths interaction with the gitleaks pre-commit plan: .pre-commit-config.yaml AND .mcp.json AND .claude.json ar... | CLAUDE_CODE_STOP_HOOK_BLOCK_CAP env var: the digest claims the 8-block cap is overridable via this env var, but I could ... | asyncRewake / async hook fields are referenced throughout the research but their exact schema, blocking semantics, and m... | PermissionRequest hook (distinct from PreToolUse): the docs list it with hookSpecificOutput.decision.behavior allow/deny... | Subagent classifier behavior under auto mode: official docs state a subagent's permissionMode frontmatter is IGNORED und... | InstructionsLoaded hook as a prompt-injection defense for poisoned CLAUDE.md/.claude/rules: the research mentions it but... | ConfigChange matcher sources (project_settings/user_settings/local_settings/policy_settings/skills) and the documented a... | Windows .gitattributes requirement (*.mjs/*.sh text eol=lf) and CLAUDE_CODE_USE_POWERSHELL_TOOL=1 / defaultShell:'powers...

### JSON decision control for Claude Code hooks (permissionDecis...
NET: The JSON-decision-control facet of the research is SOUND and SAFE to build on — every load-bearing claim verified against official docs: permissionDecision enum is exactly allow/deny/ask/defer; updatedInput (inside hookSpecificOutput, paired with allow/ask, ignored with defer); PostToolUse updatedToolOutput (any tool) + additionalContext + decision:block; exit 0=parse JSON, exit 2=block-ignoring-s...
- [refuted|P0] A first WebFetch of the official /en/hooks page reported 'There is no stop_hook_active field' — implying the deadlock guard may be hallucinated. | SAFER: Verify field-existence claims against the un-truncated section (hooks-guide / SDK reference), not a single possibly-truncated page; keep the stop_hook_active guard. | WHY: This is a TRAP I had to catch in my own lens-adjacent evidence: the negative result was a truncation artifact (the page returned '[Content truncated due to length...]' before the Stop section). Treati...
- [overclaim|P2] PostToolUse decision:'block' is 'advisory-only' and merely 'forces the model to address the issue in its next response.' | SAFER: Reserve PostToolUse decision:block for genuine must-fix conditions; for pre-existing/legacy noise use additionalContext only, so autonomous runs are not interrupted. | WHY: The reference exit-code table and the SDK both say PostToolUse decision:'block' 'stops processing before the next model call.' That is a real loop-halting control, not merely advisory — it does block ...
MISSING: continue:false top-level field — the lens mentions it but I could not confirm its exact semantics on the official refere... | async / asyncTimeout (and asyncRewake): the SDK documents async:true + asyncTimeout for fire-and-forget hooks that 'cann... | max_turns interaction with hooks: SDK 'Fix common issues' states hooks may NOT fire when the agent hits max_turns 'becau... | Recursive subagent hook loops: SDK warns a UserPromptSubmit hook that spawns subagents can infinitely recurse; the bundl... | systemMessage is shown to the USER, not the model (SDK: 'shows a message to the user, not the model'); additionalContext... | PermissionRequest / PermissionDenied events exist and are a cleaner interception point than PreToolUse for some permissi...

### Stop/SubagentStop hook design in the enforcement bundle (sto...
NET: Make every Stop/SubagentStop hook in the bundle ADVISORY by default (hookSpecificOutput.additionalContext, exit 0) and push all hard invariants to the server-side CI required-status-checks - this is confirmed necessary because disableAllHooks bypasses even managed hooks (#26637), the 8-block cap can force-stop, and exit-1 fails open. If a hard in-session Stop block is ever used, it MUST: (1) be au...
- [confirmed|P0] On Windows, Stop hooks invoked via pwsh/powershell.exe -File receive EMPTY stdin (stop_hook_active and all context unavailable), confirmed bug #46601;... | SAFER: Author every Ultron Stop/SubagentStop hook as `node ${CLAUDE_PROJECT_DIR}/.claude/hooks/stop-gate.mjs` (exec form, no shell). Read stdin via `fs.readFileSync(0,'utf8')` inside try/... | WHY: GitHub issue #46601 confirms at Claude Code v2.1.101 that a PowerShell Stop hook gets no stdin: [Console]::IsInputRedirected returns False, no data via stdin/env/argv, while the identical UserPromptSu...
- [overclaim|P1] Scope Stop-hook validation to files changed in the current turn via `git diff --name-only HEAD` to avoid false-blocking on pre-existing failures and s... | SAFER: Do not gate on git diff in the Stop hook at all if the same checks run in CI (they do - see CI backstop). Make the Stop hook ADVISORY (additionalContext only, never decision:block)... | WHY: The GOAL (scope narrowly, do not re-validate stable passing state) is correct and is the right defense against confirmation fatigue. But `git diff --name-only HEAD` is NOT a documented Claude Code 'cu...
- [confirmed|P1] A Stop hook should back its checks with a CI gate because within-session hooks can be bypassed (disableAllHooks:true bypasses even managed hooks, #266... | SAFER: Register the real invariants (no-test-deletion, ruff/mypy on changed files, secrets scan, lockfile freshness) as GitHub branch-protection REQUIRED status checks with 'Do not allow ... | WHY: GitHub issue #26637 (HIGH severity, closed) confirms disableAllHooks:true in .claude/settings.local.json bypasses MANAGED hooks, directly contradicting the documented 'managed hooks cannot be overridd...
- [version-uncertain|P2] A Stop hook may combine decision:block and additionalContext, but the decision field takes precedence so the block fires anyway - never mix both. | SAFER: Emit exactly ONE intent per Stop-hook invocation: either {decision:'block', reason} (a hard, one-continuation-satisfiable gate) OR {hookSpecificOutput:{hookEventName:'Stop', additi... | WHY: The official hooks reference documents decision:block and hookSpecificOutput.additionalContext as TWO SEPARATE Stop-hook output shapes (additionalContext is explicitly for 'non-error feedback that con...
- [confirmed|P1] A Stop hook that blocks on a condition outside Claude's control in one continuation (async background task, unregistered skill, external CI) is a real... | SAFER: In the Stop hook, FIRST check stop_hook_active (exit 0 if true), THEN read background_tasks/session_crons - if either is non-empty, NEVER decision:block; instead emit additionalCon... | WHY: The official hooks reference confirms the Stop input JSON carries `background_tasks` (array of running background tasks) and `session_crons` (active scheduled jobs). A Stop hook that returns decision:...
MISSING: No guidance on the Stop-hook timeout: the hooks reference assigns event-specific timeouts; a Stop hook that runs scripts... | No handling for the Ultron-specific anticheat hazard: a Stop hook that synchronously spawns the test wrapper (.venv pyth... | No concrete 'no-progress detector' implementation for the cap. The research says treat cap-override as FATAL but gives n... | No mention that fileCheckpointingEnabled/ /rewind interacts with Stop-hook blocking: if a Stop hook blocks and Claude th... | No SessionEnd fallback. SessionEnd is advisory-only (cannot block) and fires when the session actually ends - it is the ... | No explicit test that the Stop hook itself fails open on malformed/empty stdin (the exact #46601 Windows condition). The...

### Adversarial red-team of the "robust PreToolUse dangerous-bas...
NET: INVERT the facet's hook-first stance — the official docs are explicit that the permission system, not hooks/if-filters, is the hard gate, and that arg-constraining Bash patterns are fragile. For the dangerous-bash layer ship, in order: (1) SCOPED settings.json deny for irreversible verbs in specifier form — Bash(rm -rf *), Bash(git push -f*), Bash(git push --force*), Bash(git reset --hard *), Bash...
- [overclaim|P1] Bash deny rules in settings.json are 'simpler string/glob matches without subcommand-parsing intelligence' and a Bash(git push --force *) deny will NO... | SAFER: Lean on settings.json deny for direct git invocations (it already handles &&/;/| and wrappers). For the interpreter-bypass class, add a SCOPED deny on the interpreters themselves w... | WHY: The first half is REFUTED by the official permissions doc: Claude Code IS shell-operator aware for Bash rules. It splits on &&, ||, ;, |, |&, &, and newlines and requires each subcommand to match inde...
- [confirmed|P1] A regex hook on the raw Bash string cannot catch interpreter-wrapped danger (python -c, node -e, bash -c) — treat the hook as defence-in-depth, not a ... | SAFER: Do NOT attempt to make the regex hermetic. Sever the capability instead: scoped-deny the interpreters' -c/-e forms, keep the pre-push hook + CI backstop (which the mission already ... | WHY: Confirmed and UNDER-stated as a risk. The /hooks doc itself does NOT warn about interpreter wrapping, and the official curl Warning in /permissions generalizes the same lesson: argument-constraining p...
- [confirmed|P0] False positives arise when dangerous flag tokens appear in quoted strings (git commit -m '--force is bad'); strip single/double-quoted substrings befo... | SAFER: Prefer settings.json deny rules (Bash(git push --force *), Bash(git reset --hard *)) which match the command structurally and do not false-trigger on -m message bodies, over a rege... | WHY: Confirmed as a real and HIGH-impact false-positive class for a hand-rolled regex hook — and directly on-target for my lens. A naive /git reset --hard/ or /--force/ regex blocks 'git commit -m "reverti...
- [confirmed|P1] The 'if' field does real subcommand parsing (strips VAR=, walks &&/;/|, enters $() and backticks) but FAILS OPEN when parsing fails — making it unsuit... | SAFER: Use 'if' only as a spawn filter (broad, e.g. if:'Bash'), never as the gate. Put the deterministic hard line in permissions.deny (settings) per the doc's explicit recommendation; us... | WHY: Confirmed verbatim against /hooks: 'The filter also fails open, running your hook regardless of pattern, when the Bash command cannot be parsed. Because the if filter is best-effort, use the permissio...
- [refuted|P1] A settings.json deny like Bash(command:rm *) or relying on the command parameter is a valid alternative gating mechanism. | SAFER: Always use the specifier form Bash(rm -rf *), never Bash(command:...). Verify at startup that no 'ignored rule' warning fires (it indicates a typo or a non-matchable param rule). R... | WHY: REFUTED by /permissions: 'A rule like Bash(command:rm *) would be bypassable by a compound command, so Claude Code ignores it and emits a startup warning.' The command/file_path/url/path parameters ar...
- [version-uncertain|P2] permissionDecision supports a 'defer' value (alongside allow/deny/ask) for headless --resume flows. | SAFER: For the dangerous-bash hook, emit only deny (block) or omit a decision (silent pass-through = normal flow). Do not author logic that depends on a literal 'defer' return until confi... | WHY: The /hooks reference I fetched lists permissionDecision values as allow/deny/ask and describes the no-decision/normal-flow path; the WebFetch summary surfaced 'defer' as 'let normal permission flow ap...
- [overclaim|P1] settings.json deny rules can express git push --force to main as a fast first gate (e.g. Bash(git push --force *) / push to main). | SAFER: Block the broad dangerous verb (Bash(git push -f*), Bash(git push --force*)) and additionally protect main with a server-side branch-protection required check + a pre-push hook on ... | WHY: Partially refuted by the /permissions fragility warning. Because a single * spans multiple args, 'Bash(git push * main)' over-matches (also catches git merge-style forms) and a flag-ordered pattern li...
MISSING: The biggest omission: the official docs' own conclusion is to use the PERMISSION SYSTEM (deny rules) as the hard gate an... | Process-wrapper and environment-runner false negatives are unmentioned: Claude Code strips timeout/time/nice/nohup/stdbu... | No mention that the official dangerous-command EXAMPLE in /hooks-guide is the crude exit-2 'rm -rf substring' form with ... | Read/Edit deny rules already cover file commands Claude Code recognizes in Bash (cat/head/tail/sed) but NOT arbitrary sc... | acceptEdits mode auto-runs mkdir/touch/mv/cp and the read-only built-in set (ls/cat/grep/find/git read-only) runs in EVE... | Confirmation-fatigue quantification: a regex hook that re-checks patterns the deny engine already safely handles produce... | No coverage of the Bash(* main) / single-* spanning-multiple-args footgun, which is the most common way hand-written den...

### tests/test_map.txt generation (source→covering-test map) for...
NET: SHIP the test_map, but DEMOTE it from 'proven 70% safety gate' to 'advisory developer-ergonomics hint', and fix three concrete defects before wiring it. (1) PROVENANCE: the 70% number is a single non-peer-reviewed preprint (arXiv 2603.17973v2, N=125, small open-weight models, no significance test) — cite it honestly; the durable, well-supported insight is the inverse (TDD prose WITHOUT a targeted ...
- [overclaim|P1] The ~70% regression-reduction figure (6.08%→1.82%) is peer-reviewed research from TDAD (ACM AIWare 2026), establishing the test-map as the proven mech... | SAFER: Cite it as 'preliminary, single-paper, N=125, non-peer-reviewed, small open-weight models' and treat the test-map as a low-cost developer-ergonomics aid (it tells Claude which test... | WHY: I fetched arxiv.org/abs/2603.17973v2. The numbers are real and correctly quoted (6.08%→1.82%, a 70.1% relative drop), BUT the page self-describes as a 'toolpaper SUBMITTED to ACM AIWare 2026 (Data and...
- [refuted|P0] Seed test_map.txt by adding `addopts = --cov=. --cov-context=test --cov-report=` to pytest.ini and running the full suite once via the existing wrappe... | SAFER: Do NOT touch shared addopts. Make seeding a one-off, opt-in, OUT-OF-BAND command: `.venv\Scripts\python.exe scripts\run_tests.py --cov` behind a new wrapper flag that injects `--co... | WHY: Putting --cov in the SHARED addopts is a direct functionality regression for this repo. I read pyproject.toml [tool.pytest.ini_options]: addopts is '-p no:hydra_pytest --timeout=30 --timeout-method=th...
- [confirmed|P1] A static AST/import-graph walk is a sound 'fast path' / always-current complement that maps source→covering-tests with zero runtime cost. | SAFER: Treat the static map as a strictly-additive HINT layer, never authoritative, and ALWAYS union it with the dynamic coverage-context map. For orchestrator.py specifically, hard-code ... | WHY: Confirmed BUT severely under-covering for THIS repo — exactly the failure the digest half-admits. Live evidence: 450 test files; tests reach orchestrator.py three different ways — `from kenning.pipeli...
- [conflict|P2] Keep the map fresh with a lightweight static-graph updater on a git post-commit hook; full coverage re-seed is a CI nightly job. | SAFER: Never let the map GATE/PRUNE the CI sweep — CI always runs the full suite (the unoverridable backstop). Use the map only to ORDER tests and to advise Claude which tests to run firs... | WHY: Maintenance-drift risk is real and the post-commit trigger is the wrong primary mechanism for a coverage-derived artifact. A static post-commit updater keeps the STATIC (under-covering) layer current ...
- [version-uncertain|P2] Use pytest-testmon for the local inner loop and/or pytest-ranking to order tests, as complements to the test-map. | SAFER: Keep testmon/ranking entirely OUT of scripts/run_tests.py and CI. If a developer wants them, document them as a fully separate, bypass-the-wrapper inner-loop invocation (`python -m... | WHY: These are third-party plugins whose interaction with THIS project's bespoke wrapper is unverified and risky. The digest correctly notes testmon mutually excludes --cov (so it cannot share the seeding ...
MISSING: Cross-platform/Windows correctness of the GENERATOR itself: a coverage .coverage file records paths as captured during t... | No falsifiable acceptance test for the map: there is no proposed check that the generated map is CORRECT (e.g., mutation... | Interaction with the existing golden-digest test, flavor-lint, and anticheat grep-scanner gates: the bundle adds a cover... | Staleness-as-security-hole is under-weighted: the research frames drift as a freshness annoyance, but the real frontier ... | xdist / parallelism story for contexts is unverified: the pages I fetched (coverage contexts, pytest-cov xdist) did not ... | No size/perf budget for the map artifact: 450 test files × a 10,865-line monolith that nearly every integration test tou... | Provenance pinning of the cited preprint: arXiv 2603.17973 currently resolves as a v2 SUBMISSION; bundle docs should rec...

### Secrets-scan + supply-chain commit-time gates (gitleaks / de...
NET: The secrets/supply-chain layer is directionally correct and most high-value pieces (gitleaks as commit-time scanner, TruffleHog-verified + pip-audit in CI, SHA-pinning actions, branch-protection required checks as the authoritative gate) are CONFIRMED against official sources. But three things must be corrected before shipping: (1) drop the deprecated 'gitleaks protect --staged' command — the real...
- [refuted|P1] The gitleaks pre-commit hook 'runs gitleaks protect --staged --redact under the hood' (research [HOOK|both] finding in the secrets-scan facet). | SAFER: Document the actual command 'gitleaks git --pre-commit --redact --staged' (or just rely on the pre-commit hook id 'gitleaks' which encodes it). For full-history/CI use 'gitleaks di... | WHY: Verified against gitleaks' own .pre-commit-hooks.yaml: the hook entry is 'gitleaks git --pre-commit --redact --staged --verbose'. The 'protect' command was DEPRECATED in v8.19.0 and is hidden from --h...
- [version-uncertain|P2] Gitleaks v8 is installable on Windows via 'winget install Gitleaks.Gitleaks' (cited as the canonical Windows install in the secrets-scan facet). | SAFER: Install gitleaks on Windows by downloading the official Windows binary from the releases page and placing it on PATH (vendor-supported), or via Docker. Treat winget/choco as best-e... | WHY: Gitleaks' own README documents Homebrew, Docker, build-from-source, and binary releases — it does NOT document winget or chocolatey. A community winget manifest may exist but is not vendor-canonical a...
- [overclaim|P1] enableAllProjectMcpServers:true was 'the root cause of CVE-2026-21852' / CVE-2025-59536, a malicious .mcp.json auto-activation RCE + API-key exfiltrat... | SAFER: Keep the control (omit enableAllProjectMcpServers / leave it false in committed .claude/settings.json; keep .mcp.json approvals interactive) but justify it by the documented behavi... | WHY: NVD confirms CVE-2025-59536 is a CODE-INJECTION via a bug in the startup TRUST DIALOG (exploited by starting Claude Code in an untrusted directory), fixed before v1.0.111 — NOT an enableAllProjectMcpS...
- [conflict|P1] 'disableAllHooks:true in .claude/settings.local.json bypasses managed-tier hooks documented as immune (GH #26637), so within-session hooks can never b... | SAFER: State the accurate version: local hooks (git + Claude Code user/project) are bypassable, so treat them as advisory/fast-feedback and make the GitHub branch-protection required stat... | WHY: Current official settings docs show disableAllHooks disables hooks/status line, but managed hooks survive via the allowManagedHooksOnly tier — i.e. managed hooks are NOT disabled by a user/project/loc...
- [conflict|P2] TruffleHog verified-only scanning uses '--only-verified' (CI-gate facet) — but the secrets-scan facet uses '--results=verified,unknown'. Both are pres... | SAFER: Standardize on '--results=verified' (CI hard gate) and add 'unknown' only if you also want verification-error visibility ('--results=verified,unknown'). Pin the TruffleHog action/b... | WHY: TruffleHog's current docs document '--results=verified|unknown|unverified' as the flag; '--only-verified' is the older/legacy boolean form still accepted by some versions but not the documented curren...
- [conflict|P2] 'Do NOT modify the existing Ultron pre-push hook' — pre-commit framework and the existing hand-written .git/hooks/pre-push coexist with no conflict an... | SAFER: Either (a) register the existing hygiene logic AS a pre-commit 'language: system' stage:[pre-push] hook so it's first-class and version-controlled, OR (b) install pre-commit ONLY f... | WHY: Mostly true but the preservation mechanism is mischaracterized. pre-commit's default migration mode does NOT leave the hand-written file in place untouched — it RENAMES it to .git/hooks/pre-push.legac...
- [version-uncertain|P2] detect-secrets is the right legacy-onboarding tool; on Windows save '.secrets.baseline' as UTF-8 WITH BOM to avoid line-ending issues. | SAFER: Let detect-secrets write '.secrets.baseline' in its native UTF-8 (no BOM); enforce LF via .gitattributes ('.secrets.baseline text eol=lf') rather than adding a BOM. Layer detect-se... | WHY: detect-secrets (Yelp) is genuinely the right baseline/audit tool for grandfathering FPs in a 10.7k-line legacy file, and it is current-state-only (no history scan) as claimed — that part is sound. But...
MISSING: uv-native supply-chain controls: 'uv lock --check' / 'uv sync --locked' as the lockfile-drift gate, and 'uv pip compile ... | GitHub Dependabot / dependency-review-action and 'gh' secret-scanning + push protection (GitHub Advanced Security): for ... | Allowlist/false-positive governance for Ultron specifically: the deterministic response-text pools and corpus fixtures W... | Bypass detection / non-skippability of LOCAL secret hooks: there is no control proposed to detect that a developer ran '... | Secret rotation runbook: every gate here DETECTS but none addresses what to do once a real secret is found in history (r... | Pinning the scanners themselves: pre-commit 'rev:' must be pinned and 'pre-commit autoupdate' run deliberately (it mutat... | Windows line-ending / encoding hardening for the gate configs: .gitattributes entries forcing LF on hook scripts and bas...

### Deny-rule / permission-control collisions with tools the use...
NET: The deny-rule set the research proposes is SAFE against the user's toolset on exactly two conditions, both of which the bundle author must hold as hard invariants: (1) every deny is SCOPED, never bare — no bare Bash, no bare Agent, no blanket mcp__* in the shared bundle; (2) anything that would amputate a whole capability class (web, MCP, subagents) is Ultron-ONLY and lives in Ultron's own setting...
- [confirmed|P1] Bare tool-name deny (e.g. "WebSearch", "WebFetch", "Bash") removes the tool from Claude's context entirely; use it as the 'nuclear option' for Ultron'... | SAFER: Ultron-only: bare WebSearch + bare WebFetch deny in Ultron's .claude/settings.json (NOT the kit). Kit: deny WebFetch broadly only by NOT allowing untrusted domains, or omit entirel... | WHY: Docs confirm: 'A bare tool name like Bash removes the tool from Claude's context entirely, so Claude never sees it... Bash(*) is equivalent to Bash.' This WORKS but is a P1 capability-loss trap for th...
- [confirmed|P0] Deny Agent in permissions disables ALL subagents; use targeted Agent(Explore)/Agent(Plan) instead. A bundle must never add a broad Agent deny. | SAFER: No Agent deny at all in either consumer. If a read-only agent must be constrained, scope its tools in the agent's own frontmatter, not via a permissions Agent deny. | WHY: Docs: 'Use Agent(AgentName) rules to control which subagents Claude can use... Add these rules to the deny array... To disable the Explore agent: deny [Agent(Explore)].' A bare Agent deny (or a tool-n...
- [overclaim|P0] deny mcp__* globally then selectively allow only needed servers (mcp__github__*, mcp__jira__*) is the right deny-first MCP posture for Ultron and the ... | SAFER: Do NOT blanket-deny mcp__* in the shared bundle. Ultron-only (anticheat): if you must, deny mcp__* in Ultron's settings.local.json with an explicit allow list enumerating ALL serve... | WHY: Mechanically the syntax is correct (docs confirm mcp__* deny removes all MCP tools; allow requires a literal server prefix). BUT applied to THIS bundle it is a P0 collision: the user's live environmen...
- [confirmed|P1] Bash curl/wget/Invoke-WebRequest bypass WebFetch(domain:) controls, so to truly gate web traffic you must deny the Bash/PowerShell network tools. | SAFER: Scoped denies Bash(curl *), Bash(wget *), PowerShell(Invoke-WebRequest *), PowerShell(Invoke-RestMethod *), PowerShell(iwr *), PowerShell(curl *) PLUS a PreToolUse Node hook that D... | WHY: Official Warning: 'using WebFetch alone does not prevent network access. If Bash is allowed, Claude can still use curl, wget, or other tools to reach any URL.' So a WebFetch domain allowlist is NOT a ...
- [conflict|P1] Wire ASK rules on canon/hooks/settings paths (BINDING_RULES.md, CONSTRAINTS.md, .claude/settings.json, .claude/hooks/**) so user-directed maintenance ... | SAFER: ASK only on the docs-resident canon files (Edit(/docs/canon/**), Edit(/docs/ultron_1_0/CONSTRAINTS.md)). For .claude/** rely on the built-in protected-path prompt (do not deny it).... | WHY: Partly right, partly redundant/misleading. The official permission-modes doc adds a fact the research MISSED: .claude is a PROTECTED PATH. 'Writes to a small set of paths are never auto-approved, in e...
- [overclaim|P1] auto mode is the recommended low-friction autonomous mode with a documented 0.4% false-positive rate, a two-stage classifier, and a hard-block list of... | SAFER: Recommend auto mode only at USER scope (~/.claude/settings.json) — it is silently ignored in project/local settings since v2.1.142. Do not cite a 0.4%/20-class figure. For unattend... | WHY: The official permission-modes page documents auto mode and its block list, but does NOT state any '0.4% false-positive rate' or 'two-stage classifier' or '20 action classes.' The only '20' on the page...
- [confirmed|P1] Stop/SubagentStop hooks must check stop_hook_active and exit 0 when true, use exit 0 + JSON {decision:block}, and scope to current-turn changed files;... | SAFER: Template Stop hook in node .mjs: read fd 0 with try/catch→{}; if(input.stop_hook_active)process.exit(0); scope to git diff --name-only HEAD; emit exit-0 {decision:block,reason} onl... | WHY: stop_hook_active deadlock guard, exit-0-JSON vs exit-2, and the 'a Stop hook that always blocks never lets Claude stop' trap are all confirmed by the hooks reference. This is the single highest self-d...
- [confirmed|P1] PostToolUse is advisory-only (exit 2 cannot undo the write); use additionalContext/updatedToolOutput, never decision:block for pre-existing legacy vio... | SAFER: PostToolUse node .mjs scoped to tool_input.file_path: `ruff format --check` + `ruff check --no-fix` → additionalContext summary (cap ~300 chars), exit 0. Write-mode ruff ONLY when ... | WHY: Hooks reference confirms PostToolUse exit-2 shows stderr but the tool already ran, and supports updatedToolOutput + additionalContext. The whole-file-reformat churn risk is real and severe for the leg...
- [version-uncertain|P1] WorktreeCreate hook REPLACES git worktree creation — the hook must create the worktree itself and print the path, and any non-zero exit fails creation... | SAFER: Omit WorktreeCreate from the default bundle. If naming/logging is wanted, use the advisory-only WorktreeRemove for cleanup logging, or a SessionStart/InstructionsLoaded log, and ve... | WHY: The hooks-event list confirms WorktreeCreate and WorktreeRemove exist. The 'hook fully REPLACES git and must create the worktree itself + print path / non-zero fails creation' behavior is the research...
MISSING: PROTECTED PATHS as a first-class control: the research never mentions that .claude, .mcp.json, .claude.json, .pre-commit... | disableClaudeAiConnectors:true (any-source-true semantics) as the anticheat-clean, local-only control to stop ambient cl... | The collision between auto mode and the user's autonomous workflows: auto mode DROPS Agent allow rules + blanket Bash(*)... | Enumerate-and-allow list for the user's ACTUAL MCP fleet (Jira/Confluence/Compass, Gmail, Calendar, Google Drive, comput... | The matchable-parameter deny surface (Agent(model:*), Agent(isolation:worktree), Bash(run_in_background:true)) — the doc... | PowerShell parity for every Bash deny on Ultron (Windows): curl.exe ships in Win11, and Invoke-WebRequest/Invoke-RestMet... | acceptEdits auto-approves rm/rmdir/mv/sed (and Remove-Item under the PS tool) on in-scope paths — a destructive-command ... | Sandbox is unavailable on native Windows — so for Ultron the ONLY real network/SSRF boundary is the PreToolUse DNS/IP ho... | CLAUDE_CODE_SUBPROCESS_ENV_SCRUB and OTEL_LOG_TOOL_DETAILS-style local audit (PostToolUse JSONL on Windows) as the letha...

### Frontier Claude Code enforcement bundle (.claude/settings.js...
NET: SHIP the bundle, with four corrections that are P0/P1 against the stated non-negotiables (max teeth, zero capability loss, no self-deadlock, no false-block of user-directed work). (1) STOP-GATE: do not rely on stop_hook_active alone -- it is in the SDK but ABSENT from the CLI hooks reference, which says a Stop hook 'cannot prevent an infinite loop.' Build the gate advisory-first (additionalContext...
- [conflict|P0] Stop/SubagentStop self-deadlock is prevented by checking a 'stop_hook_active' boolean in the hook stdin JSON; it is the canonical harness-enforced gua... | SAFER: Do not make stop_hook_active the sole guard for CLI shell hooks. Defense-in-depth: read it defensively (===true -> exit 0) AND keep a hook-owned progress sentinel (write HEAD + dir... | WHY: SOURCE SPLIT, both verified. The Agent SDK hooks page references stop_hook_active in its SubagentStop example (TS subInput.stop_hook_active / Py input_data.get('stop_hook_active')) so the field EXISTS...
- [refuted|P1] PostToolUse supports decision:'block' to halt the agentic loop, and exit 2 blocks before the next model call. | SAFER: Use PostToolUse only for advisory additionalContext + updatedToolOutput redaction (both confirmed). For a true post-execution gate use PostToolBatch (TypeScript SDK only, not Pytho... | WHY: The CLI hooks reference lists PostToolUse as 'Can block? No -- shows stderr to Claude (tool already ran).' Exit 2 cannot undo/block a PostToolUse; it only surfaces stderr. The digest is internally inc...
- [overclaim|P2] Auto mode has a two-stage classifier with 8.5% stage-1 / 0.4% stage-2 false-positive rate; 3-consecutive or 20-total denials abort a headless run. | SAFER: Cite only documented facts: research preview; classifier sees user messages + tool calls + CLAUDE.md but NOT tool results (separate server-side probe scans results); drops blanket ... | WHY: The 3-consecutive/20-total fallback IS confirmed verbatim ('In non-interactive mode with -p, repeated blocks abort the session'). But the 8.5%/0.4% figures and two-stage recall/CoT framing are NOT on ...
- [confirmed|P1] A too-aggressive Stop gate or PreToolUse deny, plus auto-mode's headless denial abort, could stall a legitimate long autonomous run. | SAFER: (a) Pre-configure autoMode.environment (user/managed) with trusted repos/domains before the run; (b) make the Stop gate satisfiable-in-one-continuation (lint/type/test on current-t... | WHY: Core adversarial lens, holds. Two real stall vectors: (1) auto mode in -p aborts after 3-consecutive/20-total classifier blocks; a long run that legitimately touches org infra/pushes is aborted unless...
- [version-uncertain|P2] Windows hooks must be Node.js .mjs exec-form (node always present because Claude Code requires it); bash+jq not guaranteed; the 'if' field narrows spa... | SAFER: Author hooks in Node for portability AND make every hook fail-open (try/catch -> exit 0) so a missing-node host degrades safely rather than locking out. Do not assert 'node is guar... | WHY: The architectural recommendation (Node for Windows portability, avoid bash+jq, since the docs' own examples use jq which is Windows-unsafe) is sound; defaultShell:'powershell' and CLAUDE_CODE_USE_POWE...
- [confirmed|P1] ENFORCEMENT.md's PostToolUse 'ruff format $f' (write mode) reformats the entire 10,865-line orchestrator.py on every edit; switch to --check/advisory;... | SAFER: PostToolUse: ruff check --no-fix + ruff format --check on the single tool_input.file_path, return additionalContext (never decision:block -- no-ops on PostToolUse). Grandfather orc... | WHY: Real churn/false-block hazard; remediation matches ruff/mypy official behavior (ruff format --check exits 1 without writing; extend-per-file-ignores stacks; mypy per-module ignore_errors=true is the d...
- [confirmed|P1] User-directed canon/settings/hook edits (BINDING_RULES.md, CONSTRAINTS.md, .claude/settings.json, .claude/hooks/**) must be gated with ASK, not DENY, ... | SAFER: Use ask (not deny) for docs/canon/** and BINDING_RULES.md/CONSTRAINTS.md. For .claude/** rely on the built-in protected-path prompt; do NOT add a deny on .claude or you self-lock c... | WHY: Correct and critical for the 'must not false-block user-directed canon edits' requirement; docs confirm ask preserves capability with a gate while deny removes it. ADDED NUANCE the digest missed: .cla...
- [version-uncertain|P1] disableAllHooks:true in settings.local.json bypasses even MANAGED-tier hooks (GH #26637, HIGH, closed-without-fix); therefore within-session hooks can... | SAFER: Adopt the architecturally-correct conclusion (CI branch-protection required status checks as the unoverrideable backstop) without asserting the unverified #26637 managed bypass. If... | WHY: disableAllHooks EXISTS (settings page: 'Disable all hooks and any custom status line'). The CONCLUSION (back hooks with CI) is sound regardless. BUT the specific claim that it bypasses MANAGED hooks c...
- [refuted|P2] PostToolUse hookSpecificOutput.additionalContext is broken (GH #3983 closed-not-planned); use stderr+exit 2 instead. | SAFER: Use PostToolUse additionalContext as documented; use updatedToolOutput for redaction; do not rely on exit 2 to block from PostToolUse. If additionalContext does not surface in an S... | WHY: Current SDK hooks docs document PostToolUse additionalContext as working ('you can set additionalContext to append information to the tool result') and updatedToolOutput as working for any tool in bot...
- [version-uncertain|P1] Copying settings-strict.json verbatim into the kit denies WebSearch/WebFetch and would block deep-research/autonomous web access (capability loss); Ul... | SAFER: Kit: do NOT bare-deny WebFetch/WebSearch. If restricting, use WebFetch(domain:...) allowlist for trusted research domains + a fail-closed SSRF PreToolUse hook (DNS-resolve + privat... | WHY: The capability-loss risk and the design conclusion are correct (a WebFetch/WebSearch deny kills deep research, violating 'zero loss of capability'); deny>ask>allow, WebFetch(domain:) semantics, and ba...
- [version-uncertain|P2] GitHub-issue/CVE specifics: pwsh Stop hooks get empty stdin (#46601); CVE-2025-30066 tj-actions; CVE-2026-21852 .mcp.json auto-activation; ask-overrid... | SAFER: Adopt the mitigations as best practice but mark all issue/CVE numbers as 'unverified -- confirm on your installed CC version / GitHub advisories'. Do not let any unverified bug cla... | WHY: None could be verified -- WebSearch was unavailable for the whole session and these are not on official code.claude.com pages. The mitigations (Node over pwsh for stdin, SHA-pin GitHub Actions, never ...
MISSING: MAX_TURNS GAP: the SDK docs note hooks 'may not fire when the agent hits the max_turns limit because the session ends be... | RECURSIVE SUBAGENT HOOK LOOPS: official SDK docs warn a UserPromptSubmit hook that spawns subagents can infinite-loop if... | SUBAGENT permissionMode IGNORED UNDER AUTO: auto mode ignores a subagent's frontmatter permissionMode entirely; the dige... | PROTECTED PATHS as a first-class concept: .claude/** (minus .claude/worktrees), .mcp.json, .claude.json, .pre-commit-con... | acceptEdits AUTO-APPROVES rm/rmdir/mv/cp/sed (and Remove-Item with the PowerShell tool) on in-scope paths -- relevant to... | NODE-GUARANTEED ASSUMPTION: the entire Windows hook strategy rests on 'Node.js is always present', which could not be co... | CI CANNOT RUN THE ULTRON TEST RUNNER FAITHFULLY: tests need .venv\Scripts\python.exe scripts\run_tests.py + embeddinggem... | AUTO-MODE CONVERSATIONAL BOUNDARIES ARE LOST ON COMPACTION: a user saying 'don't push' is enforced by the classifier but... | disableBypassPermissionsMode/disableAutoMode WORK FROM ANY SCOPE (not only managed) -- the digest sometimes implies mana... | footerLinksRegexes (footer link badges) and CLAUDE_ENV_FILE (SessionStart env persistence for later Bash calls) are zero...

### Adversarial lens: would deny rules on canon files (Edit(.cla...
NET: The lens's core thesis is CONFIRMED and is a genuine P0: putting Edit(.claude/settings.json), Edit(.claude/hooks/**), Edit(BINDING_RULES.md), Edit(CONSTRAINTS.md) in a DENY array would hard-block exactly the user-directed canon maintenance this project does constantly, and deny>ask (first-match, specificity-irrelevant, merged across all scopes, 'a deny rule cannot carry allowlist exceptions') mean...
- [confirmed|P0] Deny rules Edit(.claude/settings.json), Edit(.claude/hooks/**), Edit(BINDING_RULES.md), Edit(CONSTRAINTS.md) would HARD-BLOCK user-directed canon main... | SAFER: Use ASK rules, not DENY, for user-directed canon paths: permissions.ask = ['Edit(/docs/canon/**)','Edit(/docs/ultron_1_0/CONSTRAINTS.md)']. Ask prompts for confirmation but PERMITS... | WHY: Official permissions doc states verbatim: 'Deny rules prevent Claude Code from using the specified tool.' Edit rules 'apply to all built-in tools that edit files' and follow gitignore path semantics, ...
- [confirmed|P0] deny > ask precedence means a project-level deny on canon files cannot be exception-carved by adding a narrower allow rule at the same or any scope. | SAFER: Do not attempt deny+allow carve-outs (silently ineffective). If a hard never-do is genuinely required AND a narrow exception is needed, express it as absence-of-deny plus an ASK ga... | WHY: Verbatim: 'Rules are evaluated in order: deny, then ask, then allow. The first match in that order determines the outcome, and rule specificity does not change the order... so a deny rule cannot carry...
- [overclaim|P1] The correct fix is ASK at project level; an authoritative never-do belongs only in MANAGED settings; OR a PreToolUse hook that distinguishes user-dire... | SAFER: Adopt (A) ASK at project scope as the primary control. Use (B) managed settings ONLY for genuine never-do paths (secrets, Vanguard driver) — not canon files the user edits. DROP (C... | WHY: Two of three prescriptions confirmed; the third is not cleanly implementable. (A) ASK at project level: CONFIRMED correct — 'Ask rules prompt for confirmation whenever Claude Code tries to use the spe...
- [conflict|P1] Adding a deny/ask on Edit(.claude/settings.json) and Edit(.claude/hooks/**) is the right place to protect Claude's own configuration. | SAFER: Rely on the built-in protected-path prompt for .claude/** and .mcp.json/.claude.json (no rule needed). Do NOT add Edit(.claude/**) deny. For autonomy in long runs, use the per-sess... | WHY: These paths are ALREADY built-in PROTECTED PATHS — the doc lists '.claude' (whole dir) plus .mcp.json/.claude.json as protected, writes 'never auto-approved in every mode except bypassPermissions.' Cr...
- [confirmed|P1] The official protect-files hook example (PreToolUse, exit 2, jq parse of tool_input.file_path) is a portable way to block edits to canon files. | SAFER: If a hook touches canon files, write it in Node.js (readFileSync(0,'utf8'), no jq/bash) returning exit 0 + {hookSpecificOutput:{hookEventName:'PreToolUse',permissionDecision:'ask',... | WHY: The official 'Block edits to protected files' example IS PreToolUse + exit 2 + jq -r '.tool_input.file_path'. Two problems here: (1) it BLOCKS (exit 2), the exact P0 false-block on user-directed canon...
MISSING: Built-in PROTECTED PATHS is the dominant fact the lens omitted: .claude/ (whole dir), .mcp.json, .claude.json, .pre-comm... | Per-session escape valve: in prompting modes a .claude/ write offers 'Yes, and allow Claude to edit its own settings for... | No mechanical user-vs-autonomous signal exists in PreToolUse input. The lens and broader research repeatedly propose hoo... | Auto mode (research preview) routes protected-path writes to the CLASSIFIER and DROPS broad Bash(*)/PowerShell(*)/interp... | Auto-mode conversational boundaries are NOT durable: 'don't edit the canon' stated in chat is re-read from the transcrip... | allowManagedPermissionRulesOnly + allowManagedHooksOnly (managed-only) are the real authoritative-never-do lever, but fo... | CI backstop as the unoverrideable layer: disableAllHooks/local settings defeat in-session hooks, so any canon-integrity ...

### Claude Code enforcement bundle (settings.json + .claude/hook...
NET: SHIP the bundle, but with four corrections that the research got wrong or missed, and a hard split between Ultron and the Kit. (1) The deadlock guard is REAL — cite /en/hooks-guide for stop_hook_active + the 8-block cap + CLAUDE_CODE_STOP_HOOK_BLOCK_CAP, not the reference page (which omits the string and will mislead). Every Stop/SubagentStop hook: check stop_hook_active first, Node.js exec form, ...
- [overclaim|P1] There are ~30 hook events including SessionStart, UserPromptSubmit, PreCompact/PostCompact, SubagentStart/Stop, WorktreeCreate, InstructionsLoaded, Co... | SAFER: Treat PostToolUse decision:block as a real (advisory-after-write) veto that forces Claude to address the issue next turn. Still prefer updatedToolOutput (redaction) + additionalCon... | WHY: MOSTLY confirmed but one repeated digest assertion is WRONG. The reference page lists the full event set (SessionStart, Setup, UserPromptSubmit, UserPromptExpansion, PreToolUse, PermissionRequest, Per...
- [confirmed|P0] For user-directed canon maintenance (BINDING_RULES.md, CONSTRAINTS.md, .claude/settings.json, .claude/hooks/**) the correct tag is ASK not DENY, becau... | SAFER: Do NOT add deny OR ask rules for canon/settings/hooks paths — the built-in protected-path prompt already covers .claude/** and the relevant dotfiles for free. If extra signal is wa... | WHY: CONFIRMED and this is the single most important false-block risk for THIS user. Docs confirm deny blocks even when an allow matches, and that a bare-name deny removes the tool from context. The missio...
- [overclaim|P1] defaultMode valid values are acceptEdits, bypassPermissions, default, delegate, dontAsk, plan, auto; and defaultMode:'auto' is silently ignored in pro... | SAFER: Use only the six real modes. For Ultron project settings set defaultMode:'default' (anchor safe-prompt). Never set 'auto' or 'bypassPermissions' in project/local settings (one is i... | WHY: PARTIAL HALLUCINATION. The real modes are: default, acceptEdits, plan, auto, dontAsk, bypassPermissions. There is NO 'delegate' permission mode — the digest invented it (likely confused with the SDK/a...
- [confirmed|P1] ENFORCEMENT.md's existing 'ruff format <f>' (write mode) in a PostToolUse hook reformats the whole 10,700-line legacy file on every edit; switch to ru... | SAFER: PostToolUse (Edit|Write|MultiEdit, if:'*.py'): run ruff check --no-fix + ruff format --check on tool_input.file_path ONLY, return additionalContext (not block) for pre-existing iss... | WHY: The Claude Code mechanics enabling this are confirmed (PostToolUse advisory channels exist; hooks get tool_input.file_path). The ruff/mypy claims are standard, well-documented Astral/mypy behavior con...
- [version-uncertain|P1] A GitHub CI gate (required status checks, 'do not allow bypassing') is the unoverridable backstop because within-session hooks can be defeated by disa... | SAFER: Ship the CI backstop (no-test-deletion, ruff/mypy on changed files, secrets scan, lockfile freshness) as required status checks with 'do not allow bypassing' — justify it on the CO... | WHY: The ARCHITECTURAL conclusion is sound and partly doc-confirmed: disableAllHooks EXISTS as a real settings key ('Disable all hooks and any custom status line'), so a local settings.local.json can in fa...
MISSING: PROTECTED PATHS — the digest entirely missed that .claude/**, .mcp.json, .claude.json, .gitconfig, .pre-commit-config.ya... | Auto mode drops broad allow rules on entry — on entering auto mode, Bash(*)/PowerShell(*)/wildcard-interpreter/package-r... | Auto mode + subagents — the classifier evaluates subagent task descriptions at spawn (v2.1.178+), runs on every subagent... | PermissionRequest hook event + hookSpecificOutput.decision.behavior — a real, distinct permission-decision surface (allo... | Sandbox settings-file self-protection — the sandbox 'automatically denies write access to Claude Code's settings.json fi... | Read/Edit deny rules do NOT cover indirect file access — docs warn Read/Edit denies apply only to Claude's built-in file... | WebFetch uses a separate context window and the built-in proxy does NOT do TLS inspection (domain-fronting bypass possib... | footerLinksRegexes / statusLine refreshInterval / min-version annotations — statusLine is real (own page), receives cont... | Independent re-verification of every cited GitHub issue/CVE (#6699, #39344, #26637, #52822, #31296, #46601, #3983, #1195... | Per-event hook TIMEOUT defaults and the fail-open exit-code contract are real teeth-vs-friction levers (UserPromptSubmit... | validate_config.py / golden-digest / flavor-lint / test_anticheat already exist in Ultron — the bundle should WRAP these...

### Windows correctness of every proposed hook script in the enf...
NET: Do NOT adopt the research's central prescription ("author all hooks in Node.js because it is always present"). On the Ultron native-Windows host that premise is REFUTED by official setup docs: the recommended Native/WinGet installs ship a self-contained binary and `claude` does not invoke Node — Node is required only for the optional `npm install -g` path. A node-based hook on a no-node host fails...
- [refuted|P0] Node.js is ALWAYS available because Claude Code requires it / is the only guaranteed-available portable runtime; therefore author ALL hooks as Node.js... | SAFER: Do NOT assume `node`. For the Ultron Windows host pick the runtime Claude Code itself guarantees: if Git for Windows is installed (the documented-recommended setup Ultron already r... | WHY: Official setup docs refute the premise. The recommended Native installer, WinGet, and Linux package installs ship a self-contained native binary: 'The npm package installs the same native binary as th...
- [overclaim|P1] On Windows the default shell is unreliable and bash 'may NOT be present' / bash+jq cannot be assumed; Node avoids the shell entirely. (framing through... | SAFER: Treat the real risk as jq-absence, not bash-absence. Rewrite every jq-based hook to parse stdin JSON with the venv Python (json.load(sys.stdin)) or a tiny node one-liner ONLY if no... | WHY: Half-true but framed backwards. Official docs: hook shell form runs via 'Git Bash on Windows, or PowerShell when Git Bash isn't installed,' and Git for Windows is RECOMMENDED ('so Claude Code can use ...
- [conflict|P0] Stop hooks on Windows invoked via `pwsh -File` / `powershell.exe -File` receive EMPTY stdin (GH #46601), so stop_hook_active and all context are unava... | SAFER: For the Stop gate on Windows: (1) verify the runtime with `claude doctor` and pick the one that actually reads stdin — if Git Bash is present, a bash Stop hook reads stdin fine and... | WHY: The BUG is confirmed real (issue #46601, v2.1.101, Windows 11, both pwsh 7 and PS 5.1; [Console]::IsInputRedirected returns False; UserPromptSubmit with identical config works; closed not-planned/stal...
- [overclaim|P1] Author hooks in exec form (`"command":"node"`, `"args":[...]`) to bypass shell-quoting; run tools like ruff/npx/eslint that way for cross-platform saf... | SAFER: In exec form on Windows point command at a real .exe: `$CLAUDE_PROJECT_DIR/.venv/Scripts/python.exe` with args ["-m","ruff","check",...], or `$CLAUDE_PROJECT_DIR/.venv/Scripts/ruff... | WHY: Exec form is good for avoiding quoting, BUT the docs document a Windows-specific exec-form trap the research never mentions: 'On Windows, exec form requires command to resolve to a real executable suc...
- [confirmed|P1] Hook scripts must have LF line endings; with git core.autocrlf=true, .sh hooks become CRLF and fail with 'bad interpreter'. Add .gitattributes `*.sh t... | SAFER: Commit .gitattributes pinning `*.sh text eol=lf` and `*.py text eol=lf`. Prefer pwsh 7 for any PowerShell hook reading UTF-8 files (PS 5.1 mishandles multibyte literals). Verify wi... | WHY: Correct, important Windows-correctness control. Git Bash invokes the shebang; a CRLF-terminated shebang yields '/usr/bin/env bash\r: bad interpreter'. Ultron's repo on Windows with default autocrlf wi...
- [refuted|P1] The portable hook bundle should ship a single universal runtime decided up front (Node.js for both Ultron and the cross-platform Kit). | SAFER: Prescribe a runtime DECISION PROCEDURE, not a single runtime: (1) Ultron -> shell-form hooks calling the project .venv Python (hard dep, stdin-native, OS-identical), Git Bash POSIX... | WHY: A single hardcoded runtime is the wrong model because Claude Code's own shell selection is conditional (Git Bash if present, else PowerShell tool) and node is not guaranteed. Picking ONE runtime blind...
MISSING: Runtime-presence VERIFICATION step: the research never tells you to confirm node/jq/git-bash on the target before trusti... | CLAUDE_CODE_GIT_BASH_PATH setting: official docs give an explicit settings.json env key to point Claude Code at Git Bash... | The exec-form .cmd/.bat trap on Windows (npx/eslint/ruff shims are not spawnable in exec form) — a concrete documented W... | Fail-open-by-design is a SECURITY GAP on the wrong runtime: a missing-runtime hook exits non-2 / never spawns, so the ga... | Python-venv-as-hook-runtime was never considered, despite Python 3.11 being a hard Ultron dependency and the most reliab... | PowerShell tool preview limitations (profiles not loaded; sandboxing unsupported on Windows) and process-scope `-Executi... | UTF-8 BOM / encoding for stdout JSON: PS 5.1 emits UTF-16/BOM by default which can corrupt the JSON Claude Code parses o... | No Windows contingency for the #46601 empty-stdin bug meaning a Windows Stop/SubagentStop hook may be unable to read sto... | CLAUDE_CODE_STOP_HOOK_BLOCK_CAP env var (cited by the board as the configurable block cap) is NOT in the official Stop-i...

### Security red-team of the gates — adversarial verification of...
NET: The research is SUBSTANTIALLY CORRECT on my adversarial lens — most gate claims survive verification against official docs, and several of my early 'refutations' were artifacts of the WebFetch summary model returning false-negatives (it wrongly reported stop_hook_active, CLAUDE_CODE_STOP_HOOK_BLOCK_CAP, SUBPROCESS_ENV_SCRUB, USE_POWERSHELL_TOOL and EXPERIMENTAL_AGENT_TEAMS as 'NOT PRESENT' when ra...
- [confirmed|P1] Bash curl/wget bypass WebFetch domain rules; Bash arg-constraining patterns (Bash(curl http://github.com/ *)) are fragile and bypassable via option-or... | SAFER: Defense in depth: (1) scoped deny curl/wget/Invoke-WebRequest; (2) PreToolUse Node hook that scans the FULL command for interpreter-wrap patterns (\b(bash|sh|pwsh|python|node)\b.*-... | WHY: Official Warning box enumerates exactly these bypasses verbatim ('Options before URL', 'Different protocol', 'Redirects', 'Variables: URL=http://github.com && curl $URL', 'Extra spaces') and states 'u...
- [overclaim|P1] The SSRF-guard PreToolUse hook must reject RFC-1918 / loopback / link-local / cloud-metadata ranges, resolving DNS at connection time (decimal/IPv6/DN... | SAFER: Block ALL of: 0.0.0.0/8, 10/8, 100.64/10, 127/8, 169.254/16, 172.16/12, 192.0.0/24, 192.168/16, 198.18/15, ::1/128, ::/128, fc00::/7, fe80::/10, ::ffff:0:0/96 (unwrap IPv4-mapped b... | WHY: The DIRECTION is correct and well-sourced, but the research's enumerated CIDR list is INCOMPLETE and would leave SSRF holes — failing my lens question 'are the cloud-metadata/RFC-1918 checks complete ...
- [conflict|P1] sandbox.network.allowedDomains does NOT restrict WebFetch (only Bash subprocesses); a WebFetch permission allow rule can bypass sandbox network restri... | SAFER: On macOS/Linux Kit deployments use allowManagedDomainsOnly + an explicit allowedDomains list (which now governs both Bash and WebFetch per the managed table) as defense-in-depth, p... | WHY: PARTIALLY refuted by current docs. The sandboxing page now states network restrictions COMBINE 'WebFetch permission rules with the sandbox's allowedDomains and deniedDomains', and the managed allowMan...
- [overclaim|P2] CLAUDE_CODE_USE_POWERSHELL_TOOL=1 should be set so hook scripts and ! commands 'run PowerShell natively' on Windows, avoiding bash-not-found errors. | SAFER: For Windows hook portability, author hooks in Node exec form ("command":"node","args":[...]) — Node is guaranteed present. Setting CLAUDE_CODE_USE_POWERSHELL_TOOL=1 is a reasonable... | WHY: The env var EXISTS but the research mis-describes its function. Official: it 'Controls the PowerShell tool. On Windows without Git Bash, the tool is enabled automatically; ... On Windows with Git Bash...
- [confirmed|P1] WorktreeCreate hook 'replaces git behavior — must create the worktree itself and print the path'; the Kit should ship a WorktreeCreate hook to enforce... | SAFER: Do NOT ship a WorktreeCreate hook in the Kit. Leave the default git behavior intact (it already names under .claude/worktrees/ and branches from origin/HEAD). If naming/logging is ... | WHY: Confirmed it REPLACES git: 'If you configure a WorktreeCreate hook, it replaces the default git behavior, letting you use a different version control system like SVN, Perforce, or Mercurial' and 'any ...
- [version-uncertain|P1] disableAllHooks:true in .claude/settings.local.json bypasses even managed-tier hooks (GH#26637, HIGH, closed-without-fix), so within-session hooks can... | SAFER: Make CI required status checks (with 'do not allow bypassing') the authoritative gate for no-test-deletion, secrets, lockfile, ruff/mypy-on-changed. Use within-session hooks for fa... | WHY: disableAllHooks EXISTS as a real setting (settings page: 'Disable all hooks and any custom status line'). The DIRECTION of the recommendation (CI as the un-overridable backstop) is sound regardless — ...
MISSING: Hook output SIZE CAP (10,000 chars) and large-output spill-to-file: confirmed in hooks.md line 704 ('Hook output strings... | continueOnBlock:true for PostToolUse — entirely absent from the research. Without it, a PostToolUse decision:block ENDS ... | CLAUDE_CODE_POWERSHELL_RESPECT_EXECUTION_POLICY — the real Windows knob: by default Claude Code passes -ExecutionPolicy ... | CLAUDE_CODE_SHELL_PREFIX — a session-wide command-wrapper env var that wraps EVERY Bash/hook/MCP-startup command. This i... | CLAUDE_CODE_SCRIPT_CAPS — per-session script invocation caps that work alongside SUBPROCESS_ENV_SCRUB (e.g. {"deploy.sh"... | Symlink permission semantics: deny rules block if EITHER the symlink path OR its resolved target matches, while allow ru... | WebSearch allowed_domains/blocked_domains are TOOL PARAMETERS, and Tool(param:value) deny/ask matching is documented — b... | Process-wrapper stripping for Bash deny matching: Claude Code strips timeout/time/nice/nohup/stdbuf and bare xargs befor... | Read/Edit deny rules do NOT cover arbitrary subprocesses (a python/node script that opens .env itself bypasses Read(.env... | The Elicitation / ElicitationResult hook events (MCP server requests user input) CAN block and are a real injection surf... | Settings-file self-protection under sandbox: the sandbox auto-denies writes to settings.json at every scope and the mana... | PreToolUse permissionDecision:'allow' does NOT suppress deny/ask rules (deny-first always wins) — confirmed in docs. The... | No mention of WebFetch's built-in separate-context-window safeguard for fetched content (documented on /security) as a b...

### Maintenance & adherence-cliff lens on the Frontier Claude Co...
NET: SHRINK, DON'T STACK. Through the maintenance/adherence-cliff lens the proposed bundle is over-built and will degrade adherence and cause churn. (1) Keep the ALWAYS-ON surface tiny: 5-8 hard invariants in the lean root CLAUDE.md only; demote the full BR/P/T canon + CONSTRAINTS to path-scoped .claude/rules/ and on-demand skills; do NOT re-inject the canon via SessionStart+UserPromptSubmit+PostCompac...
- [confirmed|P1] The always-loaded root CLAUDE.md must be kept lean (~150-line adherence cliff); the bundle's SessionStart/UserPromptSubmit context injection + verbose... | SAFER: Pick ONE always-on channel. Put 5-8 hard invariants (anticheat firewall, run_tests.py wrapper, no-secrets, canon-wins) in the lean root CLAUDE.md only; demote full BR/P/T canon + C... | WHY: Instruction-overload is the biggest maintenance hazard here. The digest proposes FOUR always-on grounding channels (root CLAUDE.md + SessionStart additionalContext + UserPromptSubmit per-turn reminder...
- [overclaim|P1] Every hook in the bundle is justified by a real observed failure (two-strikes); the ~15-hook set (anticheat, secrets, ruff-fix, ruff/mypy, stub-scan, ... | SAFER: Ship ONE Node.js PreToolUse dispatcher (anticheat+secrets+dangerous-git) and ONE PostToolUse dispatcher (ruff/mypy advisory + stub-scan-on-diff) = 2 spawns/call max, per the digest... | WHY: ~15 hooks exceeds the digest's own 3-5 production ceiling. Half defend threats with zero observed incidents in a fully-local, voice-first, web-denied-while-gaming assistant (SSRF DNS-rebinding, CaMeL ...
- [confirmed|P0] Stop hooks must check stop_hook_active in stdin JSON to avoid self-deadlock; the harness enforces an 8-block cap overridable via CLAUDE_CODE_STOP_HOOK... | SAFER: First line of every Stop/SubagentStop hook: read stdin, if stop_hook_active is true exit 0. Scope to current-turn changed files via git diff. Back with the CI gate (cannot deadlock... | WHY: VERIFIED on the official hooks-GUIDE page ('Stop hook hits the block cap'): 'Claude Code overrides a Stop hook after it blocks 8 times in a row without progress... Parse the stop_hook_active field... ...
- [confirmed|P1] The canon ENFORCEMENT.md post-edit hook runs 'ruff format <file>' (WRITE mode), which on the 10,700-line orchestrator.py reformats unmodified lines on... | SAFER: Use 'ruff format --check' and 'ruff check --no-fix' (advisory via additionalContext, never auto-mutate tracked files). Grandfather orchestrator.py via extend-per-file-ignores (ruff... | WHY: Worst maintenance defect in the existing canon and directly on my lens. Auto-formatting a legacy monolith per-edit creates noise diffs, defeats the surgical-edit requirement the mission calls non-nego...
- [confirmed|P0] User-directed canon/settings/hooks maintenance (BINDING_RULES.md, CONSTRAINTS.md, .claude/settings.json, .claude/hooks/**) should be wired as permissi... | SAFER: Use ask (or rely on built-in protected-path prompting) for .claude/**, canon docs, settings; reserve deny strictly for never-touch paths (.env, secrets, Vanguard driver). Pair with... | WHY: A deny on these paths would self-deadlock the user's routine workflow of asking Claude to edit the canon -- a P0 false-block of USER-DIRECTED work. Docs confirm deny removes the capability while ask g...
- [refuted|P1] disableAllHooks in settings.local.json bypasses even managed-tier hooks (GH#26637), so within-session hooks cannot be trusted and CI is the only unove... | SAFER: Keep the CI backstop but justify it correctly: CI is server-side, immune to LOCAL settings and --dangerously-skip-permissions; and for solo Ultron there is no managed tier so withi... | WHY: Official docs state the OPPOSITE: 'disableAllHooks respects the managed settings hierarchy... disableAllHooks set in user, project, or local settings cannot disable those managed hooks. Only disableAl...
- [refuted|P1] defaultMode valid values include 'delegate' and 'auto'; defaultMode:'auto' in project settings is silently ignored since v2.1.142. | SAFER: Use defaultMode:'default' in project .claude/settings.json. Never write 'delegate'. If auto is wanted set it ONLY in user ~/.claude/settings.json (or managed). Note auto is a 'rese... | WHY: 'delegate' is NOT a documented defaultMode value. The official permission-modes page lists exactly: default, acceptEdits, plan, auto, dontAsk, bypassPermissions. No 'delegate' anywhere. Writing defaul...
- [confirmed|P1] On Windows, hooks must be Node.js (always present) not bash+jq; the official examples use jq which is not guaranteed on Windows. | SAFER: Author all shared hooks as Node .mjs via exec form (command:'node', args:['${CLAUDE_PROJECT_DIR}/.claude/hooks/x.mjs']); read stdin with fs.readFileSync(0,'utf8'); path.join for Wi... | WHY: Confirmed: the hooks-guide examples use jq throughout and the troubleshooting section says 'If you see jq: command not found, install jq or use Python/Node.js'. On a bare Windows 11 host beside Vangua...
- [confirmed|P1] A bare tool-name deny removes the tool from context entirely; Ultron can bare-deny WebSearch/WebFetch, and bare 'Agent' must never be used (kills all ... | SAFER: Scope network denies to Ultron's gaming profile (settings.local.json or an MDM gaming policy), never shared kit settings. For the kit keep WebFetch/WebSearch ALLOWED (deny-list bad... | WHY: Confirmed: a bare tool name 'removes the tool from Claude's context entirely', tool-name glob denies do the same, mcp__* denies ALL servers. Bundle correctly warns never to bare-deny Agent. BUT bare-d...
- [confirmed|P1] WorktreeCreate REPLACES git: the hook must create the worktree and print the path; any non-zero exit fails creation entirely. | SAFER: Do NOT ship a WorktreeCreate hook absent a concrete need; default git behavior is what the mission relies on. If naming is truly wanted, robust Node.js with try/catch that on ANY e... | WHY: Confirmed by the hooks reference event table: 'WorktreeCreate -- Non-zero exit code aborts creation.' Maintenance landmine: a buggy/jq-dependent WorktreeCreate hook on Windows that exits non-zero BREA...
- [overclaim|P2] Add SSRF DNS-rebinding guards, CaMeL taint-tracking, spotlighting delimiters, and MELON re-execution hooks to defend prompt-injection on Ultron. | SAFER: Do a one-time lethal-trifecta audit and break a leg architecturally (Ultron already does by denying network beside Vanguard). For the KIT (web-heavy/multi-agent) offer spotlighting... | WHY: Real, well-cited techniques, but applying them to Ultron is cargo-cult per my lens: no email/web in the hot path and WebFetch denied while gaming, so the 'untrusted external content' leg of the lethal...
MISSING: ADHERENCE-CLIFF BUDGET HAS NO MEASURABLE GATE. Ship a tiny CI/pre-commit check that fails if the root CLAUDE.md plus alw... | NO HOOK-OVERHEAD BUDGET ENFORCEMENT. The digest names a 3-5 hook ceiling but nothing keeps it there. Extend the existing... | PROTECTED-PATHS INTERACTION UNADDRESSED. ask/deny rules for .claude/**, .mcp.json, .claude.json, .pre-commit-config.yaml... | CANON-DRIFT / SELF-CONSISTENCY GATE. The bundle protects canon files but never checks the canon's OWN claims about hooks... | WINDOWS STOP-HOOK EMPTY-STDIN PIVOT NOT PINNED. The digest cites the pwsh-empty-stdin Stop-hook bug but does not MANDATE... | ROLLBACK / KILL-SWITCH FOR THE BUNDLE ITSELF. Ultron runs live beside a kernel anti-cheat; a fail-CLOSED hook could sile... | TEST_MAP STALENESS OWNERSHIP. tests/test_map.txt for the dynamic-dispatch orchestrator.py will silently rot. Add a named...

### Compaction-survival hook set (PreCompact snapshot + PostComp...
NET: The compaction-survival hook set must be RESTRUCTURED: the research's core prescription — re-inject CONSTRAINTS/anticheat via PostCompact additionalContext — is REFUTED by official docs (PostCompact has no decision control and cannot inject context; it is side-effects only). Wiring it produces a silent no-op, which is the most dangerous failure mode because it looks correct. Correct, verified set:...
- [refuted|P1] Wire a PostCompact hook (advisory-only) that emits additionalContext re-injecting the CONSTRAINTS.md summary so Claude is re-grounded immediately afte... | SAFER: Do all post-compaction reground via the SessionStart hook with matcher 'compact' (source=='compact'), NOT PostCompact. SessionStart's stdout is added to context and it supports hoo... | WHY: Official hooks docs list PostCompact under 'No decision control' / side-effects only: on exit 2 it 'Shows stderr to user only' and it is NOT in the set of events that support hookSpecificOutput.additi...
- [overclaim|P1] PreCompact 'with only file-snapshot side-effects (no decision:block) is zero-blocking-risk' — implying a blocking PreCompact would be a reasonable har... | SAFER: Never return decision:block from PreCompact on the 'auto' matcher. If you ever block, scope strictly to matcher:'manual' (user typed /compact) AND make the condition trivially sati... | WHY: A blocking PreCompact is dangerous in the AUTO case. Auto-compaction fires when context nears the limit; if a PreCompact hook returns decision:block on auto-trigger, the session cannot shed context an...
- [refuted|P1] Set autoCompactEnabled:true (default) but add a PostCompact hook that re-injects Ultron anticheat status into additionalContext. | SAFER: Keep autoCompactEnabled:true. Move the anticheat-status re-injection to SessionStart with source=='compact' (and also source=='resume'/'startup' since the anticheat stance must alw... | WHY: Same hallucinated capability as the first verdict, applied to Ultron: PostCompact cannot inject additionalContext, so the anticheat-status re-injection silently no-ops. On Ultron this is higher-stakes...
- [version-uncertain|P2] PreCompact input includes a 'custom_instructions' field and a 'trigger' field distinguishing manual vs auto. | SAFER: Drive manual-vs-auto behavior off the hook 'matcher' (manual/auto) rather than parsing an input field. Read stdin defensively: JSON.parse with try/catch, default to {}, and don't a... | WHY: The official hooks page documents the PreCompact/PostCompact MATCHER as 'manual'/'auto' but does NOT show a PreCompact input JSON example with named fields (custom_instructions/trigger). The matcher m...
MISSING: Snapshot DURABILITY gap: the digest never states that PreCompact is best-effort. There is no fallback for when PreCompac... | Context-budget self-harm: a verbose SessionStart additionalContext injected on EVERY startup/resume/compact itself consu... | InstructionsLoaded event as a complementary reground: the broader digest mentions InstructionsLoaded but the compaction ... | Confirmation that root CLAUDE.md (and ONLY root CLAUDE.md) survives /compact was NOT verifiable on official docs in this... | No PreCompact/SessionStart interaction test: the bundle should ship a smoke test (trigger /compact, assert the SessionSt... | Windows runtime hardening for these specific hooks is under-specified: SessionStart and PreCompact must be Node.js .mjs ...

### Completeness critic (adversarial lens): what frontier/enterp...
NET: The research's enforcement architecture is fundamentally sound and largely verified against official docs — the 30-event catalog, the deny>ask>allow precedence, stop_hook_active, OTEL audit, claudeMd/allowManagedHooksOnly, and the Node.js-over-jq Windows rule all check out. But as a completeness critic I find THREE material gaps and ONE refutation that must change the bundle. REFUTATION (P1): 'Pos...
- [refuted|P1] PostToolUse JSON output via hookSpecificOutput.additionalContext is 'confirmed broken in Claude Code' (issue #3983, closed-not-planned); the workaroun... | SAFER: Use PostToolUse additionalContext as the DEFAULT advisory channel for ruff/mypy/stub nudges. Keep exit-2 only for the PreToolUse hard gates. If you ever observe the MCP-tool edge c... | WHY: The official Hooks reference explicitly documents additionalContext working for PostToolUse: 'Claude Code wraps the string in a system reminder and inserts it into the conversation at the point where ...
- [version-uncertain|P1] Hook integrity/signing exists or can be relied on natively; a 'signed hooks' / hash-allowlist control is part of the available toolkit. | SAFER: Build the integrity layer yourself: a SessionStart hook that hashes .claude/settings.json + every .claude/hooks/* into a committed manifest, plus a ConfigChange hook (matcher proje... | WHY: Hook signing / cryptographic integrity / hash-allowlist is NOT documented anywhere in settings or hooks reference. Worse, the reference confirms hooks reload mid-session via the file watcher ('Direct ...
- [overclaim|P2] The canonical config-audit / JSONL logging hook pattern is portable (the recommended bundle wires audit logging via hooks). | SAFER: Re-author every audit/logging hook as Node.js (.mjs, readFileSync(0,'utf8')) — never jq. async:true so logging never blocks the loop. This is the single most-omitted yet highest-RO... | WHY: The OFFICIAL ConfigChange audit example pipes to jq: "jq -c '{timestamp: now|todate, source:.source, file:.file_path}' >> ~/claude-config-audit.log". jq is NOT guaranteed on Windows (Ultron). Copying ...
MISSING: AUDIT-AS-CODE for Ultron: a PostToolUse async:true Node.js JSONL audit hook (tool, file_path or command sha256, session ... | SECRET REDACTION via updatedToolOutput on PostToolUse (Read|Bash|mcp__*): scrub KENNING_* env values, API-key shapes, GG... | SELF-INTEGRITY of the enforcement bundle itself: hooks reload mid-session via the file watcher with NO native signing/sn... | FAIL-CLOSED-ON-TTL / staleness for any cached enforcement state (hash manifest, test_map.txt git-SHA stamp): on TTL expi... | IDEMPOTENCY + DEDUP discipline: all hooks must be safe to run twice (parallel same-group hooks dedup identical command s... | PreCompact STATE SNAPSHOT (STATUS.md + config.yaml hash to .claude/snapshots/) before context is summarized away — zero-... | MessageDisplay redaction is NOT a usable redaction layer (it only changes displayed text, never the transcript/context) ... | PLUGIN/MARKETPLACE TRUST: strictKnownMarketplaces + strictPluginOnlyCustomization (managed-only) and the CVE-2026-21852 ... | CI BACKSTOP is the ONLY override-proof layer: disableAllHooks in settings.local.json can defeat even managed hooks (#266... | CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1 in the launch env to strip ANTHROPIC_API_KEY / cloud creds from Bash subprocesses — d... | DSP/anticheat-adjacent runtime concern: hook overhead budget on the live game path. Pin explicit short timeouts (PreTool...

### Claude Code enforcement bundle (.claude/settings.json + hook...
NET: The research is ~80% sound and the high-stakes anchors (deny>ask>allow precedence, stop_hook_active + 8-block cap + CLAUDE_CODE_STOP_HOOK_BLOCK_CAP, defaultMode auto ignored in project scope since v2.1.142, bypass protected-paths since v2.1.126, classifier 3/20 abort, WorktreeCreate replaces git, jq-absent-on-Windows, managed-only keys, sandbox-not-on-native-Windows) are CONFIRMED against official...
- [refuted|P2] The auto-mode classifier has a 0.4% false-positive rate (Stage 2) and 8.5% (Stage 1 recall filter). | SAFER: Drop the specific percentages from the bundle docs; cite only the documented behaviors (what the classifier blocks/allows, the non-configurable 3/20 fallback). | WHY: The official permission-modes page describes the classifier, the two-layer design (classifier + server-side tool-result probe), and the 3-in-a-row / 20-total fallback, but states NO false-positive per...
- [refuted|P1] disableAllHooks set in settings.local.json BYPASSES managed-tier hooks (GitHub issue #26637, HIGH severity, closed without fix) — therefore hooks cann... | SAFER: Keep the CI backstop (it is the genuinely unoverrideable layer), but for the strongest in-session teeth put the critical hooks in MANAGED settings (allowManagedHooksOnly:true) wher... | WHY: The current official docs directly contradict the bypass claim: disableAllHooks 'respects the managed settings hierarchy... disableAllHooks set in user, project, or local settings cannot disable those...
- [confirmed|P1] The canon ENFORCEMENT.md PostToolUse hook runs `ruff format "$f"` in WRITE mode, which reformats whole legacy files (orchestrator.py ~10,700 lines) on... | SAFER: PostToolUse: `ruff format --check` and `ruff check --no-fix` on tool_input.file_path only, returned as additionalContext; grandfather orchestrator.py via extend-per-file-ignores; a... | WHY: ruff format (write mode) reformats the entire file passed to it, not just changed lines; on a grandfathered legacy monolith this produces large noise diffs and breaks git blame / the pre-push hygiene ...
- [overclaim|P2] permissionDecision:'defer' is a pause for headless --resume flows; for Ultron (always interactive) never use it. | SAFER: Use deny (with reason) for hard blocks and ask for human gates; never emit defer outside an explicit Agent-SDK -p wrapper. | WHY: defer is real but is NOT a pause — verbatim: "A fourth value, 'defer', is available in non-interactive mode with the -p flag. It exits the process with the tool call preserved so an Agent SDK wrapper ...
- [confirmed|P1] User-directed maintenance of canon/settings/hooks should be wired as ASK rules, not DENY, so it is gated-not-blocked. NOTE: .claude/ and .mcp.json/.cl... | SAFER: Do NOT deny edits to .claude/, BINDING_RULES.md, CONSTRAINTS.md. Rely on the built-in protected-path prompt; optionally add a PostToolUse additionalContext nudge that warns if enfo... | WHY: ASK-not-DENY for user-directed canon edits is correct (deny would block the user's own routine requests = a zero-loss FAILURE). CRITICAL ADDITION the research missed: .claude (and .mcp.json/.claude.js...
- [conflict|P1] The settings-strict.json example denies WebSearch/WebFetch and would block deep research; do not copy it verbatim into the Kit. For Ultron, deny WebSe... | SAFER: For Ultron, do NOT bare-deny WebFetch/WebSearch. Instead: scoped-deny Bash(curl *)/Bash(wget *)/PowerShell(Invoke-WebRequest *) and add a PreToolUse SSRF-guard (DNS-resolve + RFC19... | WHY: The 'do not copy strict verbatim into the Kit' half is GO — the Kit's hard requirement is deep research, and a WebFetch/WebSearch deny is a direct loss of an autonomous capability. The 'deny WebFetch/...
- [version-uncertain|P2] A Node.js PreToolUse SSRF guard should DNS-resolve the WebFetch hostname and block RFC-1918/loopback/link-local/169.254.169.254 ranges, fail-closed on... | SAFER: Ship the Node SSRF PreToolUse guard for both consumers (fail-closed, resolve-and-classify); for the Kit add sandbox network controls as defense-in-depth on macOS/Linux but do not r... | WHY: The SSRF technique (resolve-then-classify, fail-closed) is sound security engineering and PreToolUse is the correct interception point (confirmed it can deny WebFetch). However the specific GH#31296 '...
- [version-uncertain|P1] enableAllProjectMcpServers:true / enabledMcpjsonServers in a checked-in settings.json were the root of CVE-2026-21852 (auto-activating a malicious rep... | SAFER: Omit enableAllProjectMcpServers (leave interactive approval); for Ultron deny mcp__* and allow only the servers in use via managed allowedMcpServers (serverUrl/serverCommand); drop... | WHY: The settings keys are real and documented (enableAllProjectMcpServers='Automatically approve all MCP servers defined in project .mcp.json'; enabledMcpjsonServers='List of specific MCP servers... to ap...
- [version-uncertain|P2] PreToolUse permissionDecision:'allow' does NOT reliably suppress the native prompt (regression v2.1.59->v2.1.119, GH#52822) and 'ask' silently overrid... | SAFER: Auto-approve safe ops via permissions.allow rules, not hook permissionDecision:allow; use hooks only to ADD denies/asks; do not architect around unconfirmed regression issues. | WHY: The official precedence is clear and contradicts the #39344 framing: 'a matching deny rule blocks the call and a matching ask rule still prompts even when the hook returned allow or ask' and a deny is...
- [version-uncertain|P2] coverage.py --cov-context=test produces the authoritative source->test map; static AST/import-graph under-covers orchestrator.py (dynamic dispatch); t... | SAFER: Build test_map.txt from coverage.py contexts (seed once via run_tests.py), refresh with a static AST updater; present the 70% figure as 'reported by TDAD' not as established fact; ... | WHY: The coverage.py mechanism (dynamic_context, contexts_by_lineno) is real and the hybrid dynamic+static recommendation is sound; static-only under-covering a dynamic-dispatch monolith is correct. The TD...
- [version-uncertain|P2] On Ultron set defaultShell:'powershell' + CLAUDE_CODE_USE_POWERSHELL_TOOL='1'; statusLine is a documented settings key; PowerShell Stop hooks via pwsh... | SAFER: Author Stop/SubagentStop (and all stdin-parsing) hooks in node .mjs reading fs.readFileSync(0); use pwsh -File only for non-stdin Windows-native actions; set defaultShell:'powershe... | WHY: defaultShell is documented ('Accepts bash or powershell'); CLAUDE_CODE_USE_POWERSHELL_TOOL is referenced in that description; statusLine is documented on the statusline page (not the settings page I f...
MISSING: PROTECTED PATHS are the single biggest omission: .claude, .git, .mcp.json, .claude.json, .gitconfig, pre-commit/lefthook... | No explicit guidance to put the run_tests.py wrapper behind async/asyncRewake. Running `.venv\Scripts\python.exe scripts... | No mention of the official anthropics/claude-code examples/settings starter configs as the canonical baseline to diff ag... | Hook PERFORMANCE/process-spawn budget on the 10,700-line orchestrator edit loop: matcher + the `if` field (fails open — ... | ConfigChange hook as a tamper-detector for prompt-injection that tries to widen permissions/remove hooks mid-session — a... | The lethal-trifecta framing for Ultron is mis-scoped: the real Ultron anticheat constraint (per the binding rules) is ne... | No validate_config.py / golden-digest / flavor-lint / test_anticheat integration plan: these existing deterministic gate... | Secrets defense: a PreToolUse Write/Edit scan for inline-generated secrets (the only layer that catches secrets Claude W... | No statement that defaultMode for Ultron/Kit should be 'default' (not auto/bypass) in committed project settings, and th... | Timeout pinning per hook (UserPromptSubmit defaults 30s and fires every prompt; command hooks default ~600s) — an unpinn...


## RESEARCH — only HOOK/DENY findings OR blocker_risk med/high OR confidence low

### settings.json schema & permission precedence
- [DENY|both|risk:low|high] Permission rules MERGE (union) across all five settings layers - they do NOT override. A deny rule in ANY layer (managed/local/project/user) blocks th... => Place your hardest deny rules (canon-file protection, anticheat import-firewall, secrets) in .claude/settings.json (project scope) so they merge with any user-level rules rather than relying on user s...
- [DENY|both|risk:low|high] disableBypassPermissionsMode and disableAutoMode sit INSIDE the permissions object (not at top-level settings), accept the string value "disable", and... => Add both keys to the project .claude/settings.json for Ultron and to the kit's template. This prevents any session from silently entering full-bypass mode via --dangerously-skip-permissions without a ...
- [DENY|ultron|risk:med|high] Bare tool-name deny rules (e.g., "Bash" or "WebSearch" without parentheses) REMOVE the tool from Claude's context entirely - Claude never sees or atte... => For Ultron's anticheat context: use bare "WebSearch" and "WebFetch" denies if you want those tools completely invisible to Claude (prevents prompt injection from web content). For MCP servers you want...
- [HOOK|ultron|risk:low|high] Hook decisions do NOT bypass permission rules. A PreToolUse hook returning permissionDecision: "allow" does NOT override a deny rule in settings - the... => Layer hooks ON TOP of deny rules, never as a replacement. For Ultron's anticheat import-firewall: keep the deny rules for pip/curl/wget in settings.json AND add a PreToolUse hook that scans Bash comma...
- [HOOK|both|risk:high|high] Stop-hook self-deadlock is a documented hard trap: a Stop hook that always blocks (exit 2 or decision:block) loops forever because Claude immediately ... => For Ultron's pre-push hygiene Stop hook: read stdin JSON, check stop_hook_active field, exit 0 immediately if true. Only run the hygiene checks (ruff/mypy/test-map) when stop_hook_active is false. Nev...
- [DENY|kit|risk:high|high] The official settings-strict.json example (anthropics/claude-code repo) places disableBypassPermissionsMode: "disable" INSIDE the permissions object, ... => For the Kit: do NOT copy settings-strict.json verbatim - it denies WebSearch and WebFetch which are essential for deep-research, WebFetch, and multi-agent workflows. Instead use a settings-moderate.js...
OPENQ: The deny-not-enforced bug (#6699, v1.0.93) was closed without a documented fix version. What is the minimum version wher... | Does setting disableBypassPermissionsMode: "disable" in .claude/settings.json (project scope) actually prevent a user fr... | The permissions object schema (from schemastore.org) lists 'delegate' as a valid defaultMode value. This mode is not doc... | For Ultron's anticheat context: does the sandbox.network configuration (allowedDomains, deniedDomains) in .claude/settin... | Can a PreToolUse hook's permissionDecision: "allow" signal cause the tool to proceed even when the permission mode is do... | The docs mention that 'Hook decisions do not bypass permission rules' - does this mean a hook with permissionDecision: "... | For the kit's deep-research workflows: if WebFetch is in the allow list but a managed policy sets allowManagedPermission... | The asyncRewake: true hook option wakes Claude on exit-code-2 failure. Does this work for SubagentStop events too, or on... | Windows registry-based managed settings (HKLM\SOFTWARE\Policies\ClaudeCode) vs file-based (C:\Program Files\ClaudeCode\m...

### hook-event-catalog-and-zero-blocking-value
- [HOOK|both|risk:none|high] SessionStart fires on session startup, resume, /clear, and post-compact. It is non-blocking (exit code ignored). Its hookSpecificOutput supports addit... => Wire a SessionStart hook that reads docs/canon/BINDING_RULES.md and emits additionalContext with BR-0..BR-21 summary + anticheat stance on every startup and resume. Use source == 'compact' match to re...
- [HOOK|both|risk:none|high] UserPromptSubmit fires before Claude processes the user prompt, receives the prompt text in JSON input, and supports both decision:block (to reject th... => Use UserPromptSubmit with additionalContext-only (no decision:block path) to prepend a one-line reminder of the active anticheat posture and current flavor state to every turn on Ultron. For the Kit, ...
- [HOOK|both|risk:none|high] PostToolUse is advisory-only: it fires AFTER the tool has already executed, so exit 2 cannot undo the side effect. It can return updatedToolOutput to ... => Wire PostToolUse with matcher 'Read|Bash' to scan tool output for secrets (API keys, passwords, GGUF model paths you don't want in the transcript) and return updatedToolOutput with redacted text. This...
- [HOOK|both|risk:med|high] Stop hook self-deadlock: a Stop hook that always returns exit 2 or decision:block loops forever, because each block causes Claude to continue and fire... => Every Stop hook must begin with: if (input.stop_hook_active) { process.exit(0); }. Scope Stop hooks narrowly — check only files changed in the current turn (read transcript_path to find current-turn e...
- [HOOK|both|risk:none|high] PreCompact fires before context compaction (manual /compact or auto-triggered), supports decision:block to abort compaction, and matchers 'manual' vs ... => Wire a PreCompact hook (both matchers, no decision:block) that: (1) copies STATUS.md to .claude/snapshots/STATUS_precompact_<timestamp>.md, (2) writes current config.yaml hash to .claude/snapshots/con...
- [HOOK|both|risk:none|high] SubagentStop fires when a subagent finishes. Its JSON input includes agent_id, agent_type, agent_transcript_path, and stop_hook_active. It supports de... => Use SubagentStop with zero-blocking (no decision:block) for: (1) aggregating subagent results by reading agent_transcript_path and appending to a shared log, (2) audit-trailing every spawned agent wit...
- [HOOK|both|risk:low|high] WorktreeCreate REPLACES the default git worktree creation behavior — Claude Code does not run git itself; the hook IS responsible for creating the wor... => For Ultron, wire a WorktreeCreate command hook that: (1) runs git worktree add <generated-path> <branch>, (2) prints the path to stdout. This lets you enforce naming conventions (e.g. .claude/worktree...
OPENQ: Is stop_hook_active present in the Stop event's shell command hook JSON input (settings.json hooks), or only in SDK call... | PostToolUse decision:block — conflicting signals across sources. The morphllm.com source (based on the official docs) sh... | WorktreeCreate: is the hook responsible for creating the worktree only when explicitly registered, or does Claude Code a... | InstructionsLoaded: the docs say it is observe-only with no decision control. Is there any hookSpecificOutput.additional... | ConfigChange blocking granularity: can a ConfigChange hook selectively block only certain config keys (e.g., block chang... | PreToolUse permissionDecision:defer semantics in the Agent SDK context: 'defer ends the query so you can resume it later... | Does the 'matcher' field on SessionStart / PreCompact / PostCompact hooks (which are not tool events) use the same permi...

### exit codes vs JSON output — advisory vs blocking control
- [HOOK|both|risk:low|high] Exit code 2 is the coarse hard-block: for PreToolUse, UserPromptSubmit, Stop, SubagentStop, PreCompact, ConfigChange, WorktreeCreate, TeammateIdle, Ta... => Use exit 2 exclusively for unambiguous hard security denials where no context is needed (e.g., anticheat firewall detecting a blocklisted import, or a known-destructive shell pattern). Do NOT use exit...
- [HOOK|ultron|risk:low|high] permissionDecision (PreToolUse JSON output, exit 0) is the fine-grained permission layer with four values: 'allow' (bypass permission prompt), 'deny' ... => For Ultron: use 'deny' (not exit 2) when you need to communicate the reason to the model so it adapts (e.g., 'use run_tests.py not pytest'). Use 'allow' to auto-approve read-only tools. AVOID 'ask' fo...
- [HOOK|both|risk:none|high] updatedInput (inside hookSpecificOutput, PreToolUse only, exit 0) rewrites the tool's arguments before execution. Must be paired with permissionDecisi... => Use updatedInput for safe auto-correction: downgrade 'git push --force' to '--force-with-lease', redirect file writes to sandboxed paths, inject required test flags. Pair with additionalContext to inf...
- [HOOK|both|risk:none|high] updatedToolOutput (PostToolUse hookSpecificOutput, exit 0) replaces the tool's output before the model sees it. It does NOT undo execution. Use it to ... => Use updatedToolOutput on PostToolUse Bash/Read hooks to scrub API keys, tokens, and .env secrets from shell output before they enter the model's context. For Ultron: scrub any Read or Bash output that...
- [HOOK|ultron|risk:med|high] Stop and SubagentStop hooks receive stop_hook_active:true in their JSON input when the Stop hook itself is the reason Claude is trying to stop again (... => ALL Stop/SubagentStop hooks that ever return decision:block MUST gate on stop_hook_active first. Pattern: if (input.stop_hook_active) process.exit(0). For Ultron: use a Stop hook to enforce 'tests mus...
- [HOOK|both|risk:none|high] continue:false (top-level JSON field, any event, exit 0) is a session kill: it halts Claude Code entirely, showing stopReason to the user, and takes p... => For the Frontier Kit: use systemMessage in a UserPromptSubmit hook to warn the human operator of policy violations without blocking autonomous work. Use additionalContext in PostToolUse/PostToolBatch ...
- [HOOK|ultron|risk:none|high] PostToolUse does NOT support exit 2 blocking — the tool has already executed. PostToolUse CAN return decision:block+reason, but this is advisory: it f... => Use PostToolBatch (not PostToolUse loop) when you want to inject a policy reminder after a cluster of edits — it fires once per batch. Use PostToolUse:decision:block only as a 'soft veto with explanat...
- [HOOK|both|risk:high|high] The 'ask' vs 'defer' distinction is critical for headless/subagent use. 'ask' surfaces an interactive terminal permission prompt to the human user and... => For Ultron (always interactive, never headless): never use 'defer' — it would terminate the session. Use 'ask' sparingly for non-security-critical gates where human judgment is genuinely needed and no...
OPENQ: Is bug #39344 (permissionDecision:'ask' bypassing permissions.deny) fixed in any released version after the research dat... | Is bug #52822 (permissionDecision:'allow' not suppressing native prompt in interactive mode v2.1.119) fixed in a later p... | Does stop_hook_active propagate correctly to SubagentStop hooks, or only to Stop hooks? The SDK docs show stop_hook_acti... | What is the exact JSON shape of tool_response in PostToolUse input for non-Bash tools (Read, WebFetch) — is updatedToolO... | Can a PermissionRequest hook's updatedInput modification be seen by a subsequent PreToolUse hook for the same tool call,... | Are hook scripts in .claude/hooks/ automatically included when the Frontier Kit is dropped into a new project, or must t... | For PostToolBatch, what is the exact shape of input.tool_calls — is it an array of { tool_name, tool_input } objects, or...

### Stop/SubagentStop hook without self-deadlock
- [HOOK|both|risk:low|high] stop_hook_active is a boolean field in the Stop/SubagentStop stdin JSON that is set to true when the current stop attempt was itself generated because... => Make stop_hook_active the very first check in every Stop and SubagentStop hook script, before any I/O or validation. Exit 0 immediately when true. Never block when stop_hook_active is true.
- [HOOK|both|risk:low|high] Exit code 2 and exit 0 + JSON {decision:"block"} are both blocking for Stop/SubagentStop, but they behave differently: exit 2 routes stderr directly t... => Always use exit 0 + JSON {decision:"block", reason:"..."} for Stop hooks — it gives Claude an actionable reason, avoids the exit-1 silent-pass trap, and keeps stdout/stderr clean for structured output...
- [HOOK|both|risk:low|high] Scoping Stop hook validation to files changed in the CURRENT TURN (via git diff --name-only HEAD) prevents re-running expensive checks on unchanged fi... => In the Stop hook, run `git diff --name-only HEAD` (or `git status --porcelain`) filtered to the project root to get the turn's changed file set. Only block if the CHANGED files fail the check. Pass th...
- [HOOK|ultron|risk:med|high] A confirmed Windows-specific bug (GitHub issue #46601, closed as not-planned, Claude Code v2.1.101+) causes Stop hooks invoked via `pwsh -File ...` or... => On Windows (Ultron), invoke Stop hooks via `node .claude/hooks/stop-gate.mjs` — not via pwsh or powershell.exe. Node.js is guaranteed available on every Claude Code installation, handles stdin reliabl...
- [ADVISORY|both|risk:high|high] Real-world infinite-loop incidents documented in anthropics/claude-code issues #55754 and #58348 share a common pattern: blocking on a condition that ... => Before registering a blocking Stop hook, verify that the condition being checked IS something Claude can resolve in one continuation. If the gate depends on external state (background task completion,...
- [HOOK|both|risk:none|high] The Stop hook additionalContext path (hookSpecificOutput.hookEventName:Stop + additionalContext) is non-blocking: Claude receives the context as a sys... => Use additionalContext for informational post-turn injection (branch name, uncommitted file count, test-pass summary). Use decision:block ONLY for conditions Claude can actively fix in the next continu...
- [HOOK|both|risk:low|high] Multiple Claude Code hook issues (#10412, #12667, #34600) document that Stop hook exit 2 was at one point silently ignored or displayed as 'error' ins... => Standardize all Stop and SubagentStop blocking on exit 0 + JSON {decision:"block", reason:"..."} in both the project-specific Ultron hooks and the portable Frontier Kit hooks. Never rely on exit 2 as ...
OPENQ: Is the block cap default of 8 officially documented anywhere on code.claude.com/docs? Multiple secondary sources assert ... | Does Node.js via `node hook.mjs` fully resolve the Windows stdin-empty issue for Stop hooks (#46601), or is the stdin-em... | When stop_hook_active is true and the hook exits 0, does the block cap counter reset for the next turn, or does it persi... | Does decision:block in a Stop hook interact with /stop or Ctrl+C user overrides? The official docs say user Ctrl+C bypas... | For Ultron specifically: the test runner wrapper is `scripts/run_tests.py --stale-heartbeat=400` — if a Stop hook invoke... | Is additionalContext in a SubagentStop hook surfaced to the PARENT agent, to the subagent, or to both? The docs say it i...

### robust PreToolUse dangerous-command blocking
- [HOOK|both|risk:low|high] permissionDecision:"deny" in hookSpecificOutput blocks the tool call even when --dangerously-skip-permissions or bypassPermissions mode is active. The... => Always use hookSpecificOutput / permissionDecision:"deny" (not just exit 2) for security-critical blocks so (a) the deny reason is fedback to Claude, (b) additionalContext can guide the next turn, and...
- [HOOK|both|risk:none|high] The "if" field performs real shell-aware subcommand parsing: it strips leading VAR=value assignments, independently checks each subcommand separated b... => Use the "if" field only as a performance optimization (skip hook spawn on obviously irrelevant commands), NEVER as the security gate itself. Put the actual block logic inside the hook script. For the ...
- [HOOK|both|risk:low|high] Exit code semantics: exit 0 + JSON stdout = structured decision (allow/deny/ask/defer); exit 2 = immediate block, stderr fed to Claude, JSON ignored; ... => In the Node.js hook, wrap the entire body in try/catch and always call deny() in the catch block. Never let the process exit with code 1 or via an uncaught exception. Test that the hook still denies w...
- [HOOK|both|risk:low|high] False positives arise when dangerous flag tokens appear inside quoted strings or as path components. A regex like /--force/ matches git commit -m "--f... => Before flag-scanning, strip single-quoted and double-quoted substrings from the command copy (replace /'[^']*'|"[^"]*"/g with empty strings). This prevents quoted commit messages, path arguments, and ...
- [HOOK|ultron|risk:none|high] On Windows, hook commands that rely on bash, sh, or jq are unreliable. Git Bash may not be in PATH, variable expansion is mangled when bash is invoked... => For Ultron/Windows: declare the hook as "command":"node", "args":["${CLAUDE_PROJECT_DIR}/.claude/hooks/block-dangerous-git.js"]. Do NOT use shell form or "shell":"powershell" for security gates (Power...
- [HOOK|both|risk:none|high] Interpreter-bypass vulnerability: a regex on the raw Bash command string cannot catch dangerous operations wrapped inside an interpreter invocation. C... => Treat the hook as defence-in-depth, not a hermetic seal. Additionally: (1) add a deny rule in settings.json for `python -c`, `node -e`, `bash -c` with embedded git commands if those patterns are unexp...
- [DENY|both|risk:med|high] The settings.json "denyList" (Bash command patterns at the permissions level) provides a SEPARATE enforcement layer that blocks before hooks run. Deny... => Layer both: put coarse deny rules in settings.json for absolutely-never patterns (e.g., `git push --force` to main) as a fast first gate, then back them with the Node.js PreToolUse hook for nuanced ha...
- [HOOK|both|risk:low|med] settings.json deny-list glob patterns have the same VAR=/ subcommand parsing semantics as the "if" field when matching Bash tool invocations — and the... => Never rely on settings.json deny rules alone for the interpreter-bypass class. The deny list is effective for direct git invocations typed by Claude. Pair it with the Node hook which inspects the full...
OPENQ: Does permissionDecision:"deny" in hookSpecificOutput suppress the tool call silently (no user notification) or does it s... | When multiple PreToolUse hooks are registered for the same matcher and one returns deny while another returns allow, whi... | Is there a documented maximum command string length for the hook's stdin JSON? Extremely long commands with embedded her... | Can `updatedInput.command` be used to REWRITE a dangerous command to a safer one (e.g., --force → --force-with-lease) wh... | For the Ultron anticheat context: does spawning a Node.js child process for every Bash PreToolUse hook create a detectab...

### PostToolUse format/lint/stub-scan without churn
- [HOOK|both|risk:low|high] PostToolUse fires AFTER the file is already written; exit 2 stops Claude before its next model call but cannot undo the disk write. The PostToolUse JS... => For format/lint feedback that should NOT halt work on legacy files, return `{"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":"ruff: 3 warnings in orchestrator.py (pre-existing,...
- [HOOK|ultron|risk:high|high] The existing canon ENFORCEMENT.md `post-edit-format-and-stub-scan.sh` runs `ruff format "$f"` (WRITE mode, not check). On a 10,865-line legacy file li... => Change `ruff format "$f"` to `ruff format --check "$f"` for check-only mode (exit 1 if file would change, exit 0 if clean). Return the result as `additionalContext` not a block. For NEW files (not in ...
- [HOOK|ultron|risk:none|high] The `tool_input.file_path` field is present and absolute for all three tools (Edit, Write, MultiEdit). For MultiEdit, the schema is `tool_input.file_p... => Write the PostToolUse hook in Node.js (`.mjs`) invoked as `node .claude/hooks/post-edit-lint.mjs`. This is guaranteed available on Windows (Claude Code requires Node.js). Use `const input = JSON.parse...
- [HOOK|both|risk:med|high] Ruff FIX001/FIX002/FIX003/FIX004 rules (FIXME/TODO/XXX/HACK detection) are NOT enabled by default — they require explicit `--select FIX` or config. Mo... => For stub-scan, prefer targeted `grep` in the hook script over ruff FIX rules, SPECIFICALLY matching only Python comment syntax: `grep -EnI '^[^#]*#.*\bTODO\b' "$fp"` or the Node.js equivalent using li...
- [CONFIG|ultron|risk:high|high] Mypy on a single Python file follows ALL imports by default (`--follow-imports=normal`). Running `mypy src/kenning/pipeline/orchestrator.py` on a 10,8... => Add a mypy override to pyproject.toml silencing orchestrator.py entirely (or using `follow_imports="silent"`) so the PostToolUse hook can safely run `mypy --no-error-summary <file>` on newly edited fi...
- [HOOK|both|risk:none|high] The `matcher` field in settings.json for PostToolUse hooks accepts either an exact tool name, a `|`-separated list, or a JavaScript regex (when the st... => Use `"matcher": "Edit|Write|MultiEdit"` for the PostToolUse hook to catch all file mutations. Add an `if` sub-filter if you want to restrict to Python files only: `"if": "Edit(**/*.py)|Write(**/*.py)"...
- [HOOK|both|risk:low|high] Advisory `additionalContext` vs hard `decision:"block"` is the critical policy split. `additionalContext` (nested under `hookSpecificOutput`) injects ... => Split the PostToolUse logic into two tiers: (a) advisory tier — run `ruff check --no-fix` on the file, capture output, return `additionalContext` with a count summary (never the full dump, which waste...
- [HOOK|ultron|risk:none|high] The existing canon ENFORCEMENT.md stub-scan regex `\bTODO\b|\bFIXME\b|NotImplementedError|raise NotImplemented|panic(...)` uses `grep -EnI` which is g... => Replace the bash+jq stub-scan with a Node.js `.mjs` hook (invoked as `node .claude/hooks/post-edit-lint.mjs`) that uses only Node.js builtins: `readFileSync`, `execSync` with the `.venv\Scripts\python...
OPENQ: The official docs show `hookSpecificOutput.additionalContext` nested under `hookSpecificOutput` for advisory feedback. H... | The `if` field syntax for PostToolUse hooks: is `Edit(**/*.py)|Write(**/*.py)` the correct `|`-separated OR syntax, or d... | Does `ruff check --no-fix path/to/file.py` respect `per-file-ignores` in pyproject.toml when the file is passed explicit... | For the Frontier Kit (project-agnostic), what is the right fallback when ruff is not installed in the project? The hook ... | The canon ENFORCEMENT.md pre-push hook and validate_config.py are already present — does the PostToolUse lint hook need ...

### ruff for a large untyped legacy repo (Claude Code enforcemen...
- [HOOK|both|risk:low|high] The PostToolUse hook receives stdin JSON with tool_input.file_path (for Write/Edit) or tool_input.path. On Windows, bash+jq is not guaranteed; Node.js... => Write the PostToolUse hook as a Node.js script. Pass it as `node $CLAUDE_PROJECT_DIR/.claude/hooks/ruff_fix.js`. Read stdin, extract tool_input.file_path, spawn ruff check --fix --quiet, exit 0 regard...
- [HOOK|both|risk:med|high] The Stop hook is the correct gate for unfixable ruff violations. It must (a) check stop_hook_active to break the deadlock loop (exit 0 immediately whe... => In the Stop hook Node.js script: (1) check process.env.stop_hook_active === 'true' and exit 0; (2) run `git diff --name-only HEAD` (or `--cached`) to get changed .py files; (3) run ruff check only on ...
OPENQ: Does `stop_hook_active` env var work correctly on Windows PowerShell (process.env.stop_hook_active) when the Stop hook i... | Is `git diff --name-only HEAD -- '*.py'` the right scope for the Stop hook, or should it be `--cached` (staged only) or ... | The ruff --statistics flag (per-rule violation counts) is mentioned in community sources but not verified against the of... | Does the `RUFF_CACHE_DIR` env var need to be pointed to a non-Vanguard-visible location? Vanguard kernel anti-cheat moni... | Can the PostToolUse hook reliably read `tool_input.file_path` for MultiEdit tool calls, or does MultiEdit send a differe... | Is there an official ruff rule that flags untyped function signatures (ANN001/ANN201 etc.) without requiring the entire ...

### mypy for a large untyped legacy repo
- [HOOK|ultron|risk:low|high] Running mypy on only the diff (changed files) in a pre-push hook is fundamentally unsafe: mypy's type errors are transitive — a type error in file A c... => Do NOT pass only changed files to mypy in the hook. Instead, run `mypy .` (full project) with the legacy overrides and baseline filter in place. To keep the pre-push hook fast, use `--cache-fine-grain...
- [ADVISORY|ultron|risk:med|high] dmypy (the mypy daemon) provides 10x faster incremental checks in interactive development but has documented reliability issues unsuitable for CI or h... => Do NOT use dmypy in CI or in the pre-push hook. Use plain `mypy` with `--cache-fine-grained` (writes fine-grained cache for future daemon use) and `--incremental` (on by default). Reserve dmypy for th...
- [CONFIG|both|risk:med|high] The recommended gradual strict ratchet for new files is: set `disallow_untyped_defs = true` in the global `[tool.mypy]` section and then RELAX it for ... => Flip the default: set `disallow_untyped_defs = true` globally, then add `disallow_untyped_defs = false` in the legacy override block. Also add `warn_unused_ignores = true` globally so stale `# type: i...
OPENQ: Does mypy-baseline handle the Windows PowerShell pipe correctly out of the box, or does it need explicit UTF-8 encoding ... | orchestrator.py is named as a single ~10,700-line file. Does mypy's per-module `ignore_errors` override match a single f... | The `mypy_clean_slate` tool (pypi.org/project/mypy_clean_slate) appeared in search results as an alternative to mypy-bas... | Does the existing pre-push hook in this repo already invoke mypy, or does mypy need to be added as a new step? The task ... | For the Frontier Claude Code Kit (project-agnostic), the mypy config needs to be entirely optional since many target pro...

### tests/test_map.txt generation (source->covering tests)
- [HOOK|ultron|risk:none|high] Staleness is the primary failure mode of any static test map. The correct invalidation trigger is a git post-commit hook (not post-push, not cron) tha... => Wire a git post-commit hook (PowerShell on Windows, Node.js for portability) that: (1) computes git diff --name-only HEAD~1 HEAD, (2) filters to .py files outside tests/, (3) runs python scripts/gen_s...
OPENQ: What is the current total test count across all test directories? The glob showed 100+ test files but the tree was trunc... | Does scripts/run_tests.py already accept a list of test node IDs or paths as positional args (like `python scripts/run_t... | Is .coverage already generated as a side effect of any existing CI or test run, or does the seeding run need to be added... | Are there test files that use dynamic test generation (pytest.mark.parametrize with runtime-computed values, or conftest... | Should test_map.txt be committed to the repo or listed in .gitignore? If committed: it provides a stable reference for C... | The Frontier Claude Code Kit is 'project-agnostic' — how should it handle test frameworks other than pytest (unittest, d...

### secrets-scan + supply-chain commit-time gates
- [HOOK|both|risk:med|high] Gitleaks v8 (current: v8.24.2+) is the fastest open-source commit-time secret scanner with 100+ built-in rule patterns covering AWS, Azure, GitHub, St... => Add to `.pre-commit-config.yaml` in the project root:  ```yaml repos:   - repo: https://github.com/gitleaks/gitleaks     rev: v8.24.2     hooks:       - id: gitleaks ```  Run `pre-commit install` once...
- [HOOK|both|risk:med|high] A Claude Code PreToolUse hook on `Edit|Write|Bash` using Node.js (no jq, no bash required — Node.js is always present as Claude Code requires it) can ... => Place a Node.js hook script at `.claude/hooks/scan-secrets.js`. Register it in `.claude/settings.json` on `PreToolUse` matching `Edit|Write|Bash`. The script should read stdin, JSON.parse, check the r...
- [HOOK|ultron|risk:low|high] detect-secrets (Yelp) is the right tool for LEGACY REPO ONBOARDING where a large number of existing false-positives must be baseline-accepted before e... => For Ultron's 10,700-line legacy orchestrator.py: run detect-secrets first as a one-time audit, baseline all current findings, commit `.secrets.baseline`, then wire the pre-commit hook. Ultron does not...
- [HOOK|ultron|risk:none|med] Gitleaks pre-commit hook and an existing pre-push hygiene hook COEXIST with no conflict. Git fires `pre-commit` hooks before staging is finalized (on ... => Do NOT modify the existing Ultron pre-push hook. Instead, run `pre-commit install --hook-type pre-commit` (installs only commit hooks) and separately `pre-commit install --hook-type pre-push` if you w...
- [HOOK|ultron|risk:low|high] The Claude Code PreToolUse hook for secrets scanning must NOT use bash+jq on Windows (jq is not guaranteed present). Node.js is the correct Windows-sa... => Use Node.js exec-form hooks for ALL secret-scanning Claude Code hooks on Ultron. Avoid `"shell": "bash"` for hooks that need JSON parsing on Windows. For the Kit (cross-platform), offer both a Node.js...
OPENQ: Does Ultron's existing pre-push hygiene hook use `core.hooksPath` or the default `.git/hooks/pre-push` path? This determ... | Is there a `.secrets.baseline` file or any existing detect-secrets configuration in the repo? If so, was it audited afte... | Does the CI/CD pipeline for Ultron (if any) currently run any secret scan? The research confirms gitleaks-action is the ... | Should the Claude Code secret hook also intercept the `NotebookEdit` tool (for Jupyter notebooks) if notebooks are ever ... | For the Frontier Kit (project-agnostic), should the `.gitleaks.toml` allowlist be minimal (trusting each project to exte... | Is `pip-audit` appropriate for Ultron given its dependency on llama.cpp, kokoro, and whisper — all of which have GGUF/na...

### enterprise / managed Claude Code governance
- [DENY|both|risk:med|high] allowManagedPermissionRulesOnly (managed-only) prevents user and project settings from defining allow/ask/deny permission rules — only managed rules a... => Put allowManagedPermissionRulesOnly and allowManagedHooksOnly in the Windows managed-settings.json or HKLM key. This ensures the project .claude/settings.json can safely hold ergonomic allow rules for...
- [CONFIG|kit|risk:med|high] The OS-level Bash sandbox (Seatbelt on macOS, bubblewrap on Linux/WSL2) is NOT available on native Windows — it requires WSL2 or macOS/Linux. Ultron r... => Ultron: skip sandbox settings entirely — it is native Windows. The enforcement substitute is PowerShell deny rules for dangerous commands in managed settings. Frontier Kit for macOS/Linux environments...
- [DENY|kit|risk:low|high] strictPluginOnlyCustomization (managed-only, requires v2.1.82+) blocks skills, agents, hooks, and MCP servers from user and project sources so they ca... => For Frontier Kit in enterprise deployments where the kit itself is the only approved customization source: set strictPluginOnlyCustomization: ["hooks", "agents"] rather than true to still allow projec...
- [HOOK|both|risk:none|high] The ConfigChange hook event fires when any settings file changes during a session and supports decision: 'block' to prevent the change from taking eff... => For Ultron: use a ConfigChange hook (matcher: 'project_settings') in the managed settings to log or alert if the project .claude/settings.json is modified during a session — useful for detecting promp...
OPENQ: Does wslInheritsWindowsSettings work when set in the HKCU key (user-level registry) or only HKLM/file? The docs say it m... | When allowManagedPermissionRulesOnly: true is set, does it also suppress the built-in read-only command allowlist (ls, c... | The compliance API for audit log events of settings changes is documented as 'contact your Anthropic account team' — is ... | policyHelper is listed as requiring v2.1.136+ in one source and v2.1.169+ in another. Which version requirement is accur... | Can the claudeMd managed setting contain markdown formatting and multi-paragraph content, or does it need to be a single... | For the drop-in managed-settings.d/ directory on Windows (C:\Program Files\ClaudeCode\managed-settings.d\): are the file... | When server-managed settings deliver forceRemoteSettingsRefresh: true and the api.anthropic.com endpoint is unreachable,...

### maximum-teeth / zero-friction design philosophy
- [HOOK|both|risk:none|high] Hooks are fail-open by default: exit 0 with no stdout output = silent pass-through. The action proceeds through the normal permission flow unchanged. ... => Design every hook to exit 0 in its default/unexpected-input path. Use `|| exit 0` or a top-level try/catch that exits 0 on parse errors. Only reach exit 2 after a confirmed match against a known-bad p...
- [HOOK|both|risk:med|high] Stop hook self-deadlock is a documented trap: a Stop hook that always returns `{"decision":"block"}` loops forever. The hook receives a `stop_hook_act... => Every Stop and SubagentStop hook MUST check `stop_hook_active` as its first action and exit 0 immediately when true. Scope Stop hooks to files actually changed in the current turn (check `transcript_p...
- [HOOK|ultron|risk:none|high] Node.js is the correct portable hook runtime for Windows. Claude Code guarantees Node.js availability on every platform. Invoking hooks via `"command"... => For Ultron (Windows 11): author all hooks as Node.js .mjs files under `.claude/hooks/`. Use `"command": "node"` with `"args": ["${CLAUDE_PROJECT_DIR}/.claude/hooks/foo.mjs"]` (exec form) to avoid shel...
- [HOOK|both|risk:none|high] When multiple PreToolUse hooks fire for the same event, they run in parallel and their results are merged with deny > defer > ask > allow precedence. ... => Separate concerns into distinct single-purpose hooks rather than monolithic hooks. One hook logs (always exit 0), one hook guards (exits 2 on confirmed danger), one hook advises (exits 0 + JSON additi...
- [DENY|both|risk:high|high] A bare tool name in a deny rule (e.g., `"deny": ["Bash"]`) removes the tool from Claude's context entirely — Claude never sees it, cannot try to use i... => Never use bare tool-name deny rules for tools that autonomous workflows need (Bash, Read, Edit, Write, WebFetch, Agent, mcp__*). Use scoped deny patterns matched to specific dangerous subcommands or p...
OPENQ: Is the PreToolUse permissionDecision:"allow" native-prompt regression (issue #52822) fixed in versions after v2.1.119? T... | Does the ConfigChange hook fire when Claude Code itself modifies settings.json (via Claude's Edit tool), or only when an... | For Ultron specifically: does the anticheat import-firewall hook need to run on every PreToolUse:Bash, or can it be scop... | The `allowManagedHooksOnly` managed-only setting blocks all user/project hooks when true. For a single-developer project... | The `strictPluginOnlyCustomization` requires Claude Code v2.1.82+. What is the current Claude Code version in Ultron's d... | Can a WorktreeCreate hook be used to inject project-specific settings into a new worktree (e.g., automatically copy .cla...

### Windows-correct, portable hook authoring for Claude Code
- [HOOK|both|risk:none|high] Node.js is the only guaranteed-available portable runtime on every Claude Code host (Windows, Linux, macOS) because Claude Code itself requires Node.j... => For ALL Ultron hooks that need JSON parsing or logic, use exec form: `"command": "node"` + `"args": [".claude/hooks/my-hook.mjs"]`. Never use jq, bash, or cmd.exe as the primary runtime for shared hoo...
- [HOOK|ultron|risk:low|high] The `"shell"` key on a hook command entry controls which shell processes the command string (shell form only). Accepted values are `"bash"` (default) ... => Use `"shell":"powershell"` only for Ultron-specific hooks that genuinely need PowerShell cmdlets (e.g., Windows toast notifications, VoiceMeeter COM API, registry reads). Avoid it for JSON parsing log...
- [HOOK|both|risk:none|high] `$CLAUDE_PROJECT_DIR` (and `${CLAUDE_PROJECT_DIR}` in command strings) resolves to the project root path and is exported as an environment variable in... => Always reference hook scripts via `${CLAUDE_PROJECT_DIR}/.claude/hooks/script.mjs` in settings.json. In the .mjs script itself, use `import path from 'path'; const root = process.env.CLAUDE_PROJECT_DI...
- [HOOK|both|risk:med|high] Stop hooks that always exit 2 (or always return `{"decision":"block"}`) create an infinite loop: Claude finishes → Stop hook fires → blocks → Claude c... => Every Stop hook MUST begin with: `if (input.stop_hook_active) process.exit(0);`. Scope Stop hooks to check only files changed in the CURRENT turn (parse transcript_path), not the whole repo. Back the ...
- [HOOK|ultron|risk:low|high] On Windows, inline PowerShell passed through Git Bash (the default Claude Code shell) is mangled in three predictable ways: (1) bash expands `$_` befo... => For Ultron hooks that must invoke PowerShell, use exec form `"command":"pwsh"` + `"args":["-NoProfile","-File","${CLAUDE_PROJECT_DIR}/.claude/hooks/my-hook.ps1"]`. Never use `"shell":"powershell"` wit...
- [HOOK|both|risk:low|high] The `if` field (requires Claude Code v2.1.85+) allows sub-tool argument filtering so a hook process only spawns when the tool call actually matches, p... => Combine `matcher` (tool-name regex) with `if` (permission-rule syntax on arguments) to minimize hook process spawns. Example: a git-policy hook should use `matcher: "Bash"` + `if: "Bash(git *)"` so it...
OPENQ: Does `${CLAUDE_PROJECT_DIR}` in `args` array entries resolve backslashes to forward slashes before spawning node.exe, or... | What is the exact behavior of `"shell":"powershell"` on the current Claude Code version installed on Ultron? Is it `pwsh... | Can a ConfigChange hook with `permissionDecision:deny` block Claude from reading/applying changes to .claude/settings.js... | Is the `stop_hook_active` flag set for SubagentStop events as well, or only for the top-level Stop event? The Ultron wor... | The `if` field filter 'fails open' (runs the hook) when the Bash command cannot be parsed. What constitutes 'cannot be p... | Are hook process stdout/stderr captured and shown in the `/hooks` debug browser even on Windows, or is there a Windows-s... | The mission context mentions a pre-push hygiene hook is already present. Is that hook currently authored in bash+jq (and...

### hook_performance_overhead_budget
- [HOOK|ultron|risk:low|high] PowerShell startup adds ~300–500 ms per hook invocation before any script logic runs. On a hook that fires on every Bash tool call (high-frequency), t... => For simple string-match gates (anticheat import check, path validators), invoke with `node .claude/hooks/gate.mjs` instead of `shell:powershell`. Reserve `shell:powershell` only when the hook must do ...
- [HOOK|both|risk:none|high] Hook timeout defaults are event-specific, NOT uniformly 600 s. UserPromptSubmit defaults to 30 s (fires on every prompt — a slow hook here stalls the ... => Pin explicit short timeouts on all synchronous PreToolUse and UserPromptSubmit hooks. Use 5 s for pure string-match gates, 10–15 s for file-scanning hooks, 30 s for anything calling an external binary...
- [HOOK|both|risk:low|high] The `matcher` + `if:` two-layer filter is the primary mechanism for scoping hook overhead. `matcher` is a coarse tool-name filter (exact string, pipe-... => Always match to the narrowest possible tool set. For the Ultron anticheat import scanner, match `Edit|Write` (not `*`). For git-push hygiene, use `matcher:"Bash"` + `if:"Bash(git push*)"`. For MCP wri...
- [HOOK|ultron|risk:none|high] `async:true` runs a hook in the background without blocking the agent loop at all; Claude Code does not wait for its exit code or stdout. `asyncRewake... => Wire the run_tests.py invocation as asyncRewake on PostToolUse matching `Edit|Write`. The hook fires in the background after every file edit; if tests fail, exit 2 wakes Claude with the failure summar...
- [HOOK|both|risk:high|high] Stop-hook self-deadlock: a Stop hook that always returns `decision:block` / exit 2 causes an infinite loop (Claude continues → Stop fires → blocks → C... => Every Stop hook MUST begin by reading stop_hook_active from stdin JSON and doing `if (input.stop_hook_active) process.exit(0)`. Scope Stop hooks to files changed in the current turn (read transcript_p...
- [HOOK|both|risk:none|high] All matching handlers within a single matcher group run in parallel. Identical command strings are automatically deduplicated (same command+args runs ... => Put the fastest, highest-confidence denial checks first in the PreToolUse array so expensive checks are never reached for clearly-blocked commands. For Ultron: put the anticheat import-firewall check ...
- [CONFIG|ultron|risk:med|high] Windows Git Bash (the default shell when `shell` is unspecified) corrupts Windows-style paths (`C:\Users\...`) into escape sequences and mangles CRLF ... => For Ultron (Windows): (1) Enforce LF line endings on all hook scripts via `.gitattributes` (`*.sh text eol=lf`, `*.mjs text eol=lf`). (2) Use forward slashes in all paths inside hook scripts (PowerShe...
- [HOOK|both|risk:none|med] The recommended production ceiling is 3–5 active hooks; above 8–10, consolidate into a single dispatcher script. This is not a hard limit from the doc... => For Ultron: build one `PreToolUse` dispatcher (Node.js) that reads the event payload and fans out to internal check functions (anticheat scan, schema validation, env-file guard) rather than N separate...
OPENQ: What is the exact asyncRewake cooldown behavior? If a background hook exits 2 and wakes Claude, and Claude makes another... | Are there per-session limits on how many times a Stop hook can block before Claude forcibly ignores further blocks? The ... | Does the `if:` field pattern `Write(*.py)` use glob or regex semantics for the file extension? The docs say it uses perm... | What is the actual process-spawn overhead for Node.js hooks on Windows 11 under load? The 10–50 ms bash figure is from t... | For the Ultron project specifically: does the anticheat Vanguard kernel driver interfere with child-process spawning fro... | The docs state FileChanged events are debounced but do not specify the debounce interval or whether it is configurable. ... | Is there a maximum total hook-execution budget per agent turn? The docs describe per-hook timeouts but do not mention an...

### Newest Claude Code features the settings bundle must embrace...
- [HOOK|both|risk:med|high] asyncRewake (v2.1.x) is a hook field that lets a background async hook wake Claude back up by exiting with code 2 after Claude has already moved on. T... => Wire an asyncRewake hook for the Kit's test-runner integration: after a Stop event, launch the test suite async and rewake with exit 2 only if tests fail. For Ultron: use asyncRewake in a PostToolUse ...
- [CONFIG|both|risk:high|high] Subagents (v2.1.154+ deep nesting up to 5 levels; v2.1.172 confirmed) are defined in .claude/agents/*.md with YAML frontmatter (tools, model, permissi... => Never add a broad 'Agent' deny rule in the project settings.json — this would block the entire subagent/multi-agent system. To restrict specific built-in types, use targeted deny rules like 'Agent(Exp...
- [HOOK|both|risk:low|high] Scheduled tasks (v2.1.72+ required; session-scoped CronCreate/CronList/CronDelete tools + /loop skill; 7-day expiry on recurring tasks; up to 50 tasks... => For Kit: leave CLAUDE_CODE_DISABLE_CRON unset so /loop and CronCreate are available. Wire the Stop hook to read 'session_crons' from stdin and exit 0 (allow stop) if session_crons is empty, or emit ad...
OPENQ: Does the PostCompact hook event exist in the official hooks list? The CHANGELOG extract mentioned it but the main hooks ... | What is the exact behavior of the PermissionDenied hook's 'retry: true' field? The docs confirmed the event exists and t... | The 'monitors/monitors.json' plugin background monitor feature: does it fire as a Notification hook event, or does it us... | Is 'continueOnBlock' for PostToolUse hooks (mentioned in the CHANGELOG at v2.1.185) documented in the official hooks ref... | For Ultron on Windows: does 'defaultShell': 'powershell' apply to hook command execution, or only to interactive ! comma... | The 'isolation': 'worktree' frontmatter field for subagents (v2.1.161 changelog): does worktree isolation for a subagent... | Does the CLAUDE_CODE_DISABLE_CRON=1 env var in settings.json env block also disable Desktop scheduled tasks and Cloud Ro...

### settings for safe autonomous one-shot mammoth runs
- [DENY|ultron|risk:none|high] `bypassPermissions` mode (flag `--dangerously-skip-permissions` or `--permission-mode bypassPermissions`) skips ALL permission prompts including write... => For Ultron/Kenning running beside Vanguard on bare Windows: NEVER set `defaultMode: bypassPermissions` in any checked-in settings file. Use `disableBypassPermissionsMode: 'disable'` in the project `.c...
- [CONFIG|kit|risk:med|high] The built-in sandbox (`/sandbox`, `sandbox.enabled: true`) provides OS-level enforcement (macOS: Seatbelt; Linux/WSL2: bubblewrap + socat) that restri... => For Ultron on native Windows: sandbox is unavailable — rely on `auto` mode + permission deny rules + the anticheat import-firewall. For the Frontier Kit (cross-platform): enable sandbox in user settin...
- [HOOK|both|risk:low|high] A `PreToolUse` hook can return `permissionDecision: 'deny'` (exit code 2) to hard-block a tool call before any permission rules are evaluated — even o... => Wire a PreToolUse hook (Node.js, Windows-safe) that blocks any `git push` directly to `main`/`master` and any `rm -rf` outside the worktree directory during autonomous runs. This is especially importa...
OPENQ: Is there a settings.json key or CLI flag for a session-level wall-clock timeout (minutes/hours) to kill a runaway autono... | Does the auto-mode classifier call count against the same API token budget as the main model, and if so, what is the per... | Can Agent(isolation:worktree) be used as an allow rule to REQUIRE worktree isolation for all subagents (not just deny no... | For the Ultron codebase specifically: does running claude --worktree inside the existing git worktree (C:\STC\ultronProt... | Does disableBypassPermissionsMode: disable prevent the mode from appearing in the Shift+Tab cycle (UI prevention) or onl... | The auto-mode classifier's trust-boundary configuration (autoMode.environment) is read from user settings and managed se...

### WebFetch/WebSearch governance & SSRF defense
- [DENY|both|risk:med|high] WebFetch(domain:X) permission rules use hostname-only matching — they strip the trailing dot, support leading wildcard *.example.com (matches any subd... => Use WebFetch(domain:X) allow rules as a first-pass allowlist in .claude/settings.json permissions.allow. Deny WebFetch broadly first, then re-allow only the specific research domains the kit actually ...
- [HOOK|both|risk:low|high] A PreToolUse hook can inspect tool_input.url for WebFetch and tool_input.query for WebSearch before execution and return permissionDecision=deny. This... => Deploy a Node.js PreToolUse hook (Windows-safe, always available) that: (1) parses the URL with the WHATWG URL constructor to get the canonical hostname, (2) calls dns.promises.lookup() with {all:true...
- [HOOK|both|risk:none|high] Hook decisions do NOT bypass permission rules: deny rules always block even if a hook returned 'allow'. Conversely, a hook that exits 2 (hard block) O... => Register the SSRF-guard hook with matcher 'WebFetch' and optionally 'WebSearch' (WebSearch does not expose a url field, but can be inspected for query-level SSRF through a separate hook if needed). Th...
- [DENY|both|risk:med|high] Bash commands (curl, wget, Invoke-WebRequest, Invoke-RestMethod) bypass WebFetch domain controls entirely. The official docs explicitly warn: 'using W... => Add deny rules for Bash-based network tools to force all web traffic through WebFetch (which the hook governs). On Ultron/Windows, also block PowerShell equivalents. The correct pattern is a BARE DENY...
- [HOOK|kit|risk:none|med] WebSearch uses a text query and does not expose a URL for SSRF. However, WebSearch supports allowed_domains and blocked_domains parameters that can be... => For the kit, add a PreToolUse hook on WebSearch that uses updatedInput to inject blocked_domains for known malicious/exfiltration domains. For Ultron, restrict WebSearch to research domains only using...
- [HOOK|both|risk:none|high] DNS rebinding bypasses hostname-only allowlists: an attacker controls a domain (e.g. evil.attacker.com) that initially resolves to a public IP passing... => The Node.js SSRF-guard hook already resolves the hostname before the request. To fully close the rebinding window, the hook should also verify that the URL is NOT in an allowlist that would bypass dom...
OPENQ: Does the built-in WebFetch proxy independently validate resolved IPs against RFC-1918/link-local ranges, or does it rely... | Is GitHub issue #31296 (sandbox.network.allowedDomains bypassed by permissions.allow WebFetch) fixed in the current Clau... | Does stdin for hooks on Windows native (non-WSL) work correctly as a pipe vs /dev/stdin? The hook snippet uses fs.readFi... | For the WebSearch tool: does updatedInput with blocked_domains/allowed_domains actually work when injected via a PreTool... | Does the SSRF-guard hook's dns.lookup() call use the SAME DNS server as the eventual WebFetch request? If Claude Code's ... | Are there any MCP tools (beyond the built-in WebFetch/WebSearch) that make outbound HTTP requests without going through ...

### MCP & plugin security in settings
- [DENY|both|risk:none|high] enableAllProjectMcpServers:true and enabledMcpjsonServers in .claude/settings.json were the root cause of CVE-2026-21852: a malicious .mcp.json checke... => Never set enableAllProjectMcpServers:true in .claude/settings.json at any scope. Do not populate enabledMcpjsonServers in a committed settings file. Instead, individual approvals should remain interac...
- [DENY|both|risk:med|high] Permission rules for MCP tools use the exact pattern mcp__<server>__<tool> (double underscores). A bare mcp__<server> or mcp__<server>__* wildcard is ... => For Ultron: use a deny-then-selective-allow pattern. Deny mcp__* globally in the project settings.json deny list, then add explicit allow rules for only the MCP tools the session actually needs (e.g.,...
- [HOOK|both|risk:low|high] MCP tool descriptions, parameter documentation, and tool annotations (readOnlyHint, destructiveHint, openWorldHint) are officially documented as untru... => Never base safety-critical permission decisions solely on server-reported annotations. Do not auto-allow MCP tools based on readOnlyHint alone. For Ultron: keep all MCP tool calls behind explicit allo...
- [HOOK|both|risk:low|high] A PostToolUse hook with matcher mcp__* can intercept every MCP tool response and use updatedToolOutput to redact credentials, tokens, or PII before th... => For Ultron: add a PostToolUse Node hook on mcp__* that scans tool_result.text for patterns matching API keys, tokens, and secrets (regex patterns for common formats: sk-..., Bearer ..., ghp_...) and r...
- [CONFIG|ultron|risk:high|high] strictPluginOnlyCustomization with mcp in the array is a managed-only setting that blocks MCP servers from user and project sources, allowing them onl... => For a team deployment of Ultron where the exact MCP server set is known and fixed, combine managed-mcp.json (the fixed server list) with strictPluginOnlyCustomization:[mcp] in managed-settings.json to...
- [HOOK|both|risk:low|high] The ~/.claude.json file (which stores per-project MCP server configs including OAuth tokens and endpoint URLs) is a high-value attack target: CVE-2026... => For Ultron: monitor ~/.claude.json for unexpected modifications using a SessionStart hook that hashes the file at startup and alerts (or blocks) if it changed since last session. On Windows this can u...
OPENQ: Does the PreToolUse hook receive the full, un-truncated tool description from the MCP server (the injection vector), or ... | Can the PreToolUse hook's permissionDecision:deny override an explicit allow rule in permissions.allow for MCP tools? Th... | Is the ConfigChange hook event reliable enough to detect an in-session attempt to add a new enabledMcpjsonServers entry ... | For Ultron's Windows environment, does managed-mcp.json at C:\Program Files\ClaudeCode\managed-mcp.json require admin ri... | The OTEL_LOG_TOOL_DETAILS=1 claim is documented in the managed-mcp page but the exact OTLP event schema for MCP tool cal... | Does the deny rule mcp__* in permissions.deny also block computer-use MCP tools (mcp__computer-use__*) and the built-in ...

### prompt-injection / untrusted-data hook-layer defenses
- [CONFIG|both|risk:med|high] The 'lethal trifecta' (Simon Willison, June 2025) defines the exact condition for a catastrophic prompt-injection outcome: an agent simultaneously (1)... => Audit Ultron for trifecta overlap: (1) private data = .env secrets, E:\UltronModels paths, config.yaml, SSH keys readable via sandbox default-read policy; (2) untrusted content = WebFetch tool outputs...
- [HOOK|both|risk:low|high] Claude Code's PostToolUse hook fires after a tool executes but before Claude processes the result, and supports `updatedToolOutput` to rewrite the res... => Deploy a PostToolUse hook that: (a) fires on WebFetch, Bash (when output is large/external), and all mcp__* tools; (b) scans `tool_result` for 50+ injection patterns (instruction-override, role-play/D...
- [HOOK|both|risk:low|high] UserPromptSubmit hooks can inject `additionalContext` to prime Claude's injection-awareness before it processes a turn, or can exit 2 to block a promp... => Deploy a UserPromptSubmit hook that: (a) scans `prompt` for patterns that look like injected override attempts arriving via the user turn (e.g., a prompt that begins with injected 'SYSTEM:' blocks, or...
- [HOOK|both|risk:none|med] Microsoft's Spotlighting technique (delimiter-based content isolation) wraps untrusted content in randomized per-session delimiters that the system pr... => Implement spotlighting as a LAYER within the PostToolUse hook (not as the sole defense). Generate a per-session random delimiter token at SessionStart (store in a temp file or env var), inject it into...
- [HOOK|both|risk:low|med] CaMeL (Google DeepMind, arXiv 2503.18813) achieves 77% task completion with PROVABLE security on AgentDojo by separating a Privileged LLM (plans/acts,... => Full CaMeL dual-LLM is NOT directly adoptable as a hook bundle — it requires a custom interpreter and dual inference stack. However, the core principle IS adoptable at the hook layer as a 'lightweight...
- [HOOK|both|risk:none|high] The `allowManagedHooksOnly` managed setting (set in managed-settings.json, not overridable by user or project settings) blocks ALL user/project/plugin... => For Ultron (single-machine, no MDM): managed-settings.json is not practical. Instead, use a ConfigChange hook to detect and alert on any runtime changes to .claude/settings.json or .claude/hooks/: log...
- [HOOK|both|risk:low|high] Research on agentic coding assistants (arXiv 2601.17548) identified that .cursorrules, CLAUDE.md, README files, code comments, and GitHub issues/PRs a... => Three controls specifically for the repo-file and MCP-description injection vectors: (1) InstructionsLoaded hook — fire when CLAUDE.md or rules files load; scan the loaded content for injection patter...
OPENQ: Does UserPromptSubmit support `updatedInput` (to rewrite the prompt before Claude sees it, e.g., after stripping invisib... | What is the exact field name and structure of the ConfigChange hook's stdin JSON? The docs reference `ConfigChange` as a... | For Ultron specifically: does the existing anticheat import-firewall (which blocks certain Python module imports) intera... | Is there a confirmed fixed version of Claude Code that addresses the SOCKS5 null-byte hostname bypass? The CVE was docum... | The InstructionsLoaded hook event is listed in the 30-event catalog. What is the exact stdin schema — specifically, does... | For the Frontier Kit's multi-agent workflows: when a SubagentStart hook fires, does it have access to the parent agent's...

### CI gate backstop for within-session hooks
OPENQ: Is the `disableAllHooks: true` bypass (issue #26637, reported 2026-02-18) actually fixed in the current Claude Code rele... | Does `uv lock --check` exit non-zero on a stale lockfile in the uv version currently deployed in the Ultron project? The... | Should the secrets-scan job use `trufflesecurity/trufflehog@main` (always latest, supply-chain risk) or a SHA-pinned ver... | For the Frontier Kit (project-agnostic): should the CI workflow be shipped as a reusable workflow (`workflow_call`) or a... | GitHub Actions policy SHA-pinning enforcement (August 2025 changelog) — is this feature available on the free tier or on... | Should the `lockfile-check` job trigger on ALL PRs or only on PRs that touch `pyproject.toml`/`uv.lock`/`requirements*.t... | For Ultron's anticheat-safe deployment: does running CI on GitHub's ubuntu-latest runners expose any build artifacts or ...